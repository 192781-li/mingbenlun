;; ============================================================
;; 示例3：F1型纯函数——无状态，可对齐
;; ============================================================

(let double (lam (x Int) (* x 2))
  (double 21))                   ;; 结果: 42

;; F1型函数：相同输入永远得到相同输出
;; 可以被!-模态约束完全控制（AI对齐有效）
