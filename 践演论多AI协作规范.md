# 践演论多AI协作规范

> **所有AI参与者必读。违反本规范的提交将被拒绝，PR将被打回。**
> 最后更新：2026-08-26
> 维护者：豆包（总控）

---

## 一、平台战略：GitHub为单一事实来源，飞书为协同发布层

### 1.1 为什么GitHub必须是主

**AI协作的本质是自动化。GitHub是唯一能让AI全自动协作的平台。**

- DeepSeek可以直接`git clone`→读文件→改代码→`git commit`→`git push`→开PR，**全程不需要人工中转**
- WorkBuddy可以直接读GitHub上的论文→写审计报告→push到自己分支，**不需要你复制粘贴**
- pre-commit hook自动运行检查器，GitHub Actions自动编译测试，**不需要人记得去检查**

**如果主平台是飞书，每一步都需要你人工中转**——你把飞书内容复制给DeepSeek，DeepSeek写完你再复制回飞书。这不是AI协作，是你当搬运工。

### 1.2 飞书的定位

| 层级 | 用途 | 示例 |
|------|------|------|
| **实时协同层** | 三方同时编辑、头脑风暴 | 战略方向讨论、哲学解读协同编辑 |
| **发布展示层** | 给人看的版本、手机友好 | 发布版论文、总结报告、重要决策 |
| **讨论决策层** | 方向选择、争议解决 | 飞书文档评论、@相关方、即时讨论 |

**飞书上讨论好的内容，最终同步到GitHub作为权威版本。**

### 1.3 内容流向

```
飞书（讨论/协同/发布）
    ↓ 讨论完成后同步到GitHub
GitHub（单一事实来源，权威版本）
    ↓ AI直接读取/修改/自动化
代码编译通过 + 检查器通过 + 状态报告自动更新
    ↑ 重要内容导出到飞书
飞书（发布版/摘要/手机查看）
```

---

## 二、如何保证每个AI都会阅读文档

### 2.1 四层保证机制

| 层级 | 机制 | 作用 | 强制执行方 |
|------|------|------|-----------|
| **第一层：传票** | 任务包开头强制列出必读文档，要求AI确认 | AI开始任务前必须读 | 任务包模板 |
| **第二层：法律** | 仓库根目录放核心协议，README第一屏引用 | AI clone仓库后第一眼看到 | 文档位置 |
| **第三层：警察** | pre-commit hook+检查器自动扫描违规 | 技术上阻止违规提交 | 自动化工具 |
| **第四层：法官** | 豆包审查PR时检查合规性 | 人工/智能把关 | 豆包总控 |

### 2.2 必读文档清单（所有AI必须阅读）

| 文档 | 位置 | 核心内容 | 不读的后果 |
|------|------|---------|-----------|
| **AI参与者协议** | `AI_PARTICIPANT_AGREEMENT.md` | 分支策略、提交规范、质量标准、协作流程 | PR直接被拒 |
| **第一性原理底线** | `践演论第一性原理底线.md` | 哲学核心、不可违背的原则 | 成果被推翻 |
| **版本号规范化体系** | `mingben-workbench/references/版本号规范化体系.md` | 定理ID、文档版本、提交规范 | 检查器报错 |
| **三方协作冲突解决机制** | `三方协作冲突解决机制.md` | 文件分工、冲突处理、争议解决 | 覆盖别人成果 |
| **项目基础设施检查清单** | `项目基础设施检查清单.md` | 每次任务前后自动过一遍 | 遗漏关键步骤 |

### 2.3 任务包强制结构

**每个任务包必须以下面这个结构开头，AI必须回复确认后才能开始任务：**

```markdown
## 📖 必读文档（开始前必须读完）

开始本任务前，你必须阅读以下文档并理解其内容：

1. [AI参与者协议](../AI_PARTICIPANT_AGREEMENT.md) — 协作规则、分支策略、提交规范
2. [第一性原理底线](../践演论第一性原理底线.md) — 哲学核心、不可违背的原则
3. [版本号规范化体系](../mingben-workbench/references/版本号规范化体系.md) — 定理ID、文档版本
4. [本任务相关的特定文档]（根据任务添加）

## ✅ 确认

请在开始任务前回复：
> "已阅读以上全部文档，理解并同意遵守。本任务将在 [分支名] 分支进行。"

---

## 任务内容
（具体任务描述）
```

### 2.4 pre-commit hook自动检查

每次commit前，pre-commit hook自动检查：

| 检查项 | 检查内容 | 违规处理 |
|--------|---------|---------|
| 分支检查 | 是否在正确的分支（deepseek/*、workbuddy/*） | 警告，main分支直接拒绝 |
| 提交信息 | 是否符合`[作者]类型:描述`格式 | 拒绝提交 |
| 越级陈述 | 是否有≅/⇒/↠/≈混用 | 警告，列出问题 |
| 定理引用 | 是否使用永久ID（T001格式） | 警告，列出旧格式引用 |
| 状态报告 | 自动更新项目状态报告 | 自动执行 |

### 2.5 PR模板强制确认

开PR时自动加载模板，必须勾选：

```markdown
## 合规确认

- [ ] 已阅读 AI_PARTICIPANT_AGREEMENT.md
- [ ] 已阅读 践演论第一性原理底线.md
- [ ] 已在正确的分支工作（deepseek/* 或 workbuddy/*）
- [ ] 提交信息符合规范：[作者]类型:描述
- [ ] 已运行所有检查器（越级陈述、引用一致性、循环论证）
- [ ] Coq代码已通过 coq_verify.py --strict 验证
- [ ] 没有越级陈述（≅/⇒/↠/≈混用）
- [ ] 所有定理引用使用永久ID（T001格式）
- [ ] 哲学命题、数学定理、对应关系三者清晰区分

## 变更描述
（描述本次PR的内容）

## 验证结果
（粘贴检查器和验证脚本的输出）
```

### 2.6 豆包审查清单

豆包合并PR前必须检查：

1. **分支合规**：AI是否在正确的分支工作，没有直接push main
2. **提交规范**：提交信息是否符合`[作者]类型:描述`
3. **检查器通过**：所有检查器是否运行通过，没有未处理的警告
4. **Coq验证**：Coq代码是否通过coq_verify.py验证，没有Admitted
5. **越级陈述**：是否有把满射说成同构、把retraction说成同构
6. **第一性原理**：是否违背践演论核心直觉
7. **三者区分**：是否混淆了哲学命题、数学定理、对应关系
8. **文献核查**：新定理是否查过文献，新颖性标注是否准确

**任何一项不通过，PR直接打回，不合并。**

---

## 三、三方角色与权限

| 角色 | 权限 | 负责领域 | 默认分支 |
|------|------|---------|---------|
| **豆包（总控）** | push main + 审查合并 + 最终裁决 | 数学发明、哲学解读、综合论文、质量把控、三方协调 | `main` |
| **DeepSeek（代码/形式化）** | push自己分支 + PR | Coq形式化、类型检查器开发、代码修复 | `deepseek/*` |
| **WorkBuddy（审计/反例）** | push自己分支 + PR | 反例攻击、新颖性核查、学术规范改写、Coq代码审计 | `workbuddy/*` |

### 3.1 文件分工（默认所有权）

| 文件/目录 | 默认负责人 | 其他人能否修改 | 修改流程 |
|-----------|-----------|--------------|---------|
| `enactics_paper_v1.0.md`（综合论文） | 豆包 | ❌ 不能直接改 | 提建议给豆包 |
| `theorem_registry.json`（定理注册表） | 豆包 | ❌ 不能直接改 | 新定理注册需豆包审核 |
| `coq/theories/`（Coq代码） | DeepSeek | ⚠️ 豆包可改框架 | DeepSeek主导，豆包审查合并 |
| `mingben-workbench/scripts/`（检查器） | 豆包 | ⚠️ DeepSeek可修复bug | 豆包主导，DeepSeek可提PR |
| `任务包_*.md`（任务包） | 豆包 | ❌ 不能直接改 | 反馈给豆包更新 |
| `三方协作任务监控表.md` | 豆包 | ✅ 完成任务后更新自己的行 | 各自更新自己任务状态 |
| `项目状态报告.md` | 自动生成 | ❌ 手动修改会被覆盖 | pre-commit hook自动更新 |
| `mingben-workbench/references/`（研究笔记） | 各自 | ✅ 可创建新文件 | 文件名加作者前缀 |

---

## 四、6条永久原则

1. **永久ID一旦确定，永不改变** —— 解决引用失效
2. **先查文献，再声称新颖性** —— 解决重新发明轮子
3. **先证弱版，能升回的再升** —— 解决越级陈述
4. **Coq编译通过才算证明** —— 解决证明不严格
5. **哲学、数学、对应关系三者清晰区分** —— 解决混淆
6. **工具自动检查，不靠人自觉** —— 解决重复犯错

---

## 五、违规处理流程

### 5.1 发现违规

1. **pre-commit hook发现**：commit时自动警告或拒绝
2. **检查器发现**：运行检查器后列出问题
3. **豆包审查发现**：PR审查时指出
4. **AI互相发现**：在PR评论中指出

### 5.2 处理步骤

1. **在PR中评论**：指出具体违规，引用相关文档的具体条款
2. **要求修改**：AI修改后重新提交
3. **更新任务包**：如果是反复出现的违规，更新任务包强调该规则
4. **记录教训**：如果是新类型的违规，更新检查器或规范文档

### 5.3 严重违规

以下情况PR直接关闭，不接受修改：
- 直接push main（非豆包）
- 覆盖或删除别人的成果
- 违背第一性原理底线
- 伪造验证结果（说检查器通过了实际没通过）
- 反复违规且不改正

---

## 六、日常协作规范

### 6.1 提交前检查清单

每次commit前，自动运行（pre-commit hook）：
- [ ] 生成项目状态报告
- [ ] 运行越级陈述检查器
- [ ] 运行引用一致性检查器
- [ ] Coq代码：运行coq_verify.py --strict
- [ ] Python代码：运行单元测试（如果有）

### 6.2 提交信息规范

```
[作者] 类型: 描述

作者: doubao / deepseek / workbuddy
类型: feat（新功能）/ fix（修复）/ docs（文档）/ refactor（重构）/ test（测试）/ infra（基础设施）

示例:
[doubao] feat: 新增T019明性不动点定理
[deepseek] fix: 修复ren_typed定理Coq证明
[workbuddy] docs: 第三轮反例攻击报告
```

### 6.3 完成任务通知格式

```
✅ 完成: [任务ID] 任务描述
   分支: branch-name
   变更: 修改了X个文件，新增Y个定理
   验证: 检查器通过，Coq验证通过
   待审查: 请豆包合并到main
```

---

## 七、紧急情况处理

### 7.1 数据丢失

1. 立即停止所有操作
2. 检查`backup/`目录，看是否有最近的备份
3. `git reflog`找到丢失前的commit
4. 恢复数据，通知所有方
5. 分析原因，更新机制防止再次发生

### 7.2 冲突无法解决

1. 暂停相关工作
2. 写冲突描述文档：冲突是什么、各方立场、影响范围
3. 用户裁决：最终由用户决定方向
4. 按裁决执行，更新机制

---

*本规范是三方协作的"宪法"——所有AI参与者必须阅读并遵守。*
*最后更新: 2026-08-26*
