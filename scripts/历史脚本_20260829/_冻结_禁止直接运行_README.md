# ⚠ 历史脚本·冻结，禁止直接运行（2026-08-30）

本目录4个脚本（progressive_v2 / progressive_analyze / batch_analyze / analyze_dialog）是 8/29 一次性递进分析的产物，已完成历史使命，**存在三个硬缺陷，禁止再直接运行**：

1. **不记 usage/代谢**：不写 token 消耗，事后对不上"能量去哪了"；
2. **留痕不全**：多数只 print 到 stdout，不写结构化日志，实例结束即蒸发；
3. **重试空转风险**：以"回答≥500字"为成功判据，thinking-max 下易反复重发整个大上下文，造成大进小出。

**8/30 15:00–16:00 6254万token蒸发事件即此类无留痕调用所致（详见《明旭的记忆/DeepSeek调用统一收口纪律》）。**

需要复用其多点采样、递进上下文逻辑时：改造为走统一入口 `scripts/ds_collab.py`（自动记代谢日志、带空转熔断、产出强制沉积），不要在此直接改了就跑。
