# S06-math 分支残留文件评估报告

> 评估日期：2026-09-14
> 评估人：S00 巡检（代行）
> 评估对象：s06-math 分支「残留 6 个 main 没有的独有文件」处置方案
> 性质：**只评估、不操作**。未执行任何删除 / 归档 / 历史重写。

---

## 一、结论（1–3 句）

**任务前提已过期：这 6 个文件在 s06-math 上已经不存在，无需再处置。**
经核实，s06-math 当前 HEAD 已从任务书所记的 `c100fde` 前进到 `6f3de69`，main 也从 `14118c7` 前进到 `4fbe930`；这 6 个大文件**既不在 s06-math 工作树、也不在 git 跟踪树、也不在磁盘上**，且 s06-math 相对 main 的独有跟踪文件数为 **0**。它们早已在 2026-09-02 ~ 09-13 的三轮「大文件保密迁移 + 防回流」中被移到**私有档案库 mingbenlun-archive**，主库仅留 INDEX 索引。

**唯一仍未关闭的口子是 git 历史**：这些 blob 仍可从 main 的公开历史中 `git show <旧commit>:<路径>` 恢复（约 44MB 原始对话 + 27MB 自动报告），是否做 filter-repo 历史重写，仍需作者拍板——本报告不动。

---

## 二、现场核实（与任务书逐条对照）

| 任务书记载 | 现场核实结果 |
|---|---|
| s06-math HEAD = `c100fde` | 实际 HEAD = `6f3de69`（`c100fde` 已是其父提交） |
| main HEAD = `14118c7` | 实际 main = `4fbe930`（本地），已多次前进 |
| 6 个文件「残留」在 s06-math | 6 个文件在 s06-math 工作树、`git ls-files`、`c100fde:<路径>` 中**全部不存在** |
| s06-math 有 main 没有的独有文件 | `comm -23 <(ls-tree s06-math) <(ls-tree main)` 输出为**空**——独有跟踪文件数 = 0 |
| overclaim_report.md 待确认去留 | 已在档案库 `auto_reports/overclaim_report.md`（27M），主库 `.gitignore` 第 80 行已覆盖 |

> 任务书里的 4 个文件名是**历史旧称/误写**，并非仓库真实路径：
> 「七全天**天文**记录」→ 实为「七八天**聊天**记录」；
> 「思想大乱**斗**」→ 实为「思想大乱**炖**」；
> 「**全网**深推…名实之**辩**」→ 实为「**八月**深推…名实之**辨**」；
> 「**阶缘**原点起」→ 实为「**阶级**原点起」。
> 用任务书的旧名做 `git log` 查无提交记录；用真实名才能在历史中查到。行数（53705 / 107783 / 62690 / 112888）完全对得上，可确认是同一批文件。

---

## 三、6 个文件逐个核查

### 1. 另一个对话_七八天聊天记录_20260829.md（任务书旧名「七全天聊天文记录」）

- **大小**：7.0M（档案库实测）
- **内容性质**：原始用户↔智能体对话导出，时间戳 2026/08/21 起，主题为「哲学家的隐含本体论 / 名实之辨」元哲学讨论。**与第 4 个文件（八月深推）开头完全一致**，二者内容重叠（七八天聊天是八月深推的子集或并行导出）。
- **查重**：主库无正文；主库 `docs/raw_materials/硬核对话记录/INDEX.md` 已登记，指向档案库 `raw_dialogues/`。
- **gitignore**：`.gitignore:81` 已覆盖真实名。
- **处置建议**：**保留在私有档案库，主库不动**。重叠问题另案处理（见第五节），本任务不删。

### 2. 思想大乱炖_20260612-0630_53705行.md（任务书旧名「思想大乱斗」）

- **大小**：6.1M，53,705 行
- **内容性质**：原始对话导出，2026/06/12 起，首轮即产出《缄默意识：一个基于生命本源性的哲学新范式》提纲。
- **查重**：主库无正文；INDEX.md 已登记。
- **gitignore**：`.gitignore:82` 已覆盖。
- **处置建议**：**保留在私有档案库**。

### 3. 电脑辩证生命论形式化_20260727-0826_107783行.md

- **大小**：14M，107,783 行（6 个文件里最大）
- **内容性质**：原始对话导出，2026/07/27 起，从「辩证唯物主义 vs 历史唯物主义」切入的 Coq 形式化辩证长对话。
- **查重**：主库无正文；INDEX.md 已登记（迁移日期 2026-09-02）。
- **gitignore**：`.gitignore:83` 已覆盖。
- **处置建议**：**保留在私有档案库**。

### 4. 八月深推_隐含本体论到名实之辨_20260821-0829_62690行.md（任务书旧名「全网深推…名实之辩」）

- **大小**：7.0M，62,690 行
- **内容性质**：原始对话导出，2026/08/21~08/29，主题即隐含本体论→名实之辨。
- **查重**：主库无正文；INDEX.md 已登记。
- **gitignore**：`.gitignore:84` 已覆盖。
- **处置建议**：**保留在私有档案库**。与第 1 个文件开头重复，建议作者日后决定是否合并去重（不影响本次结论）。

### 5. 超长主对话_阶级原点起_20250129-20260830_112888行.md（任务书旧名「阶缘原点起」）

- **大小**：9.6M，112,888 行（行数最长）
- **内容性质**：**全库最长主对话**，从 2025/01/29 豆包首次打招呼、用户第一问「中国有资产阶级吗」开始，一直到 2026/08/30。是发生史级别的原始语料。
- **查重**：主库无正文；INDEX.md 已登记（2026-09-02 迁移，把史前原点前推至 2025/01/29）。
- **gitignore**：`.gitignore:85` 已覆盖。
- **处置建议**：**保留在私有档案库**。价值最高的一份原始语料，绝不能回流公开主库。

### 6. overclaim_report.md（任务书记 26.5MB）

- **大小**：27M，**398,740 行**（2026-09-12 已更新为最新完整版）
- **内容性质**：**自动生成**的越级陈述检查报告（由 `scripts/质量检查/overclaim_checker.py` 产出），扫描全库 56,958 条问题（88,384 绝对化表述 / 9,822 新颖性声称 / 8,518 可变定理编号…），**无作者手写正文**，纯工具产物。
- **查重**：主库无正文；档案库 `auto_reports/overclaim_report.md` 为最新版；档案库 INDEX 注明「只增不改，新版替换旧版并标注」。
- **gitignore**：`.gitignore:80` 已覆盖（根目录 `overclaim_report.md`）。
- **处置建议**：**保留在私有档案库**。此类报告可随时由脚本重新生成，无需进主库；主库只留「脚本在哪、怎么跑」的说明即可。

---

## 四、整体处置建议

| 项 | 建议 | 理由 |
|---|---|---|
| s06-math 工作树 | **不做任何操作** | 6 个文件已不在树上；分支相对 main 独有文件数为 0，已是干净状态 |
| 主库 main 当前树 | **不做任何操作** | 大文件已在 4729e9b / dacfb25 / 304381a 三次迁移中从树里删除，INDEX.md 索引齐全 |
| 私有档案库 mingbenlun-archive | **维持现状** | 6 个文件已在位、有 README + INDEX 双索引、行数/大小可对上 |
| .gitignore | **已覆盖，无需改**（可选增强见下） | 第 80–85 行逐条命中真实文件名 |
| 5M 大小门 | **继续生效** | 已在 s00_sync_branches 第五层拦截，commit s00-sync-size-guard-20260913 记录在案 |

**可选的小加固（不强制、可走同 PR）**：`.gitignore:80-85` 用的是迁移后的真实文件名。若担心任务书那种旧称/误写名被误放回来，可以在这 6 行下面补一行通配（例如 `docs/raw_materials/硬核对话记录/**/*_*行.md` 配合大小门），但**当前没有任何旧称文件存在**，纯防御性。本次不动。

---

## 五、留作者定调点（必须用户拍板，本报告不代决）

1. **【唯一实质未决项】git 历史是否重写**：这 5 个对话 + 1 个报告的 blob 仍可从 main 公开历史恢复
   - 引入提交：`5faac04`（16 文件改名入库）、`88da048`、`d05cbb3`（分站同步再引入）
   - 删除当前树提交：`4729e9b`（S05 大文件迁移 P0–P2）、`dacfb25`（6 个 >5M 移出 + 防回流）、`304381a`（#152 保密回流修复）
   - 后果：任何能 clone 公开仓 `192781-li/mingbenlun` 的人，`git show 4729e9b^:docs/.../电脑辩证生命论形式化_...107783行.md` 仍能拿到完整 14MB 原文。档案库 INDEX 第 63 行自己也标注了这一点。
   - 处置选项：(a) 维持现状（接受公开历史可恢复）；(b) 跑 `git filter-repo` 或 BFG 把这些 blob 从全部分支历史抹掉（**不可逆、force-push、所有协作者需重新 clone**）。**本报告不擅动**。

2. **【数据完整性确认】**：档案库副本是否为最终版？commit `4729e9b` 已记录「文件已验证 md5 一致并存于档案库」，本报告未重算 md5。如作者要求，可补一轮 md5 对账。

3. **【重叠去重】**：第 1 个（七八天聊天记录）与第 4 个（八月深推）开头逐字相同，疑似同一批对话的两次导出。是否合并/去重，作者定；不影响归档安全。

4. **【overclaim_report 生命周期】**：27MB 的自动报告是否每次巡检都重跑、旧版是否要在档案库里轮转清理？目前档案库规则是「只增不改、新版替换旧版」，本次已是最新版。

---

## 六、失败项与原因

| 项 | 结果 | 原因 |
|---|---|---|
| 在 s06-math 上 `ls -lh` 6 个文件 | 全部 No such file | 文件已不在该分支/磁盘（见第二节） |
| 用任务书旧名查 `git log` | 无记录 | 旧名是误写，非仓库真实路径；用真实名才查到 |
| 其他 | 无 | 其余检查（大小、内容头、gitignore、查重、档案库对账）均成功 |

---

## 七、本次评估未触碰的红线（自查）

- [x] 未执行 `git filter-repo` 或任何历史重写
- [x] 未删除任何用户文件
- [x] 未把 >5M 文件引入 main（本 PR 只加这一份小报告）
- [x] 未动主工作树（`/home/user/mingbenlun` 当前在 `s00-paper-material-20260914` 且有暂存改动，本报告在独立 worktree `/home/user/mingxu-worktrees/s06-eval` 上完成）
- [x] 每条 git 命令均在对应 worktree / `git -C <仓库>` 下执行

---

## 附：本次核查用到的关键命令与事实

- 分支：s06-math = `6f3de69`；main = `4fbe930`；s06 worktree = `/home/user/mingxu-worktrees/s06`
- 独有文件对账：`comm -23 <(git ls-tree -r s06-math --name-only|sort) <(git ls-tree -r main --name-only|sort)` → 空
- 档案库：`/home/user/mingbenlun-archive`（private，github.com/192781-li/mingbenlun-archive）
- 主库索引：`docs/raw_materials/硬核对话记录/INDEX.md`（S05 信息分站维护，2026-09-02/09/12 三批迁移记录在案）
- 大小门规范：`docs/协作机制/仓库优化方案与大文件管理规范.md` 第 31–32 行（>5M 必须迁档案库）
