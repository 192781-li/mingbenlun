# 反身思考记录：DeepSeek OB007策略执行失败分析

**时间**：2026-09-02 11:30
**任务**：L2 substitution_general证明（按S01 OB007补充研判策略）
**状态**：DeepSeek执行失败，证明被截断且x=k case陷入无限循环

## 一、S01的策略回顾

S01在commit ae0c6dd中完成了TASK-S04-011（OB007补充研判），核心策略：

1. **关键辅助引理：set_none_insert_at_subst**
   ```coq
   Lemma set_none_insert_at_subst : forall Gamma T k m x z,
     x <> k -> z <> x ->
     get (set_none (insert_at k T Gamma) x) z =
     get (set_none Gamma (subst_name m k x)) (subst_name m k z).
   ```
   解决第二个use关系（y的）的上下文变换问题。

2. **POut case策略**：
   - 第一个use用get_insert_at_lt/gt处理
   - 第二个use用set_none_insert_at_subst处理
   - body部分：IHQ上下文可能不匹配，需证明上下文在自由变量位置等价

3. **哲学注释**：代换是操作角色的替换，use关系代换保持反映操作权转移的一致性

## 二、DeepSeek执行失败分析

### 失败现象
- 输出被截断（truncated），36425字符，max_tokens=15000不够
- 花费$0.016982（比之前的调用贵很多）
- x=k的case陷入无限循环的assert：
  ```coq
  assert (Hget1' : get Gamma k = Some (Some T0)).
  { rewrite Hget1''. auto. }
  assert (Hget1'' : get Gamma k = Some (Some T0)).
  { rewrite Hget1'. auto. }
  ...（重复几十次）
  ```
- 这是DeepSeek在x=k的case中卡住了，无法证明T'=T，陷入了无意义的assert循环

### 失败原因
1. **x=k的case复杂**：需要从Hget1（get (insert_at k T Gamma) k = Some (Some T')）证明T'=T。这需要分析insert_at的定义，在位置k插入的是Some T，所以T'=T。但DeepSeek没有正确处理这个。
2. **max_tokens不够**：证明很长，15000 tokens被截断了。
3. **body部分的IHQ上下文不匹配**：S01已经指出这个问题，但DeepSeek没有解决。

### 教训
1. **不要一次叫DeepSeek写完整证明**：应该每次只处理一个case，编译通过再继续。
2. **x=k的case需要自己处理**：这个case的关键是证明T'=T，可以通过simpl和inversion完成。
3. **set_none_insert_at_subst引理可能是对的**：DeepSeek写的这个引理的证明（99行）看起来可能是对的，可以先验证并添加到Layer2.v中。
4. **body部分需要新策略**：IHQ的上下文不匹配问题，可能需要证明一个"上下文在自由变量位置等价"的引理，或者修改substitution_general的陈述。

## 三、新策略

### 步骤1：验证并添加set_none_insert_at_subst引理
- 仔细检查DeepSeek写的set_none_insert_at_subst证明
- 如果正确，添加到Layer2.v中，编译验证
- 如果有问题，自己重写证明

### 步骤2：自己写substitution_general的证明框架
- 对Q归纳
- PVar/PZero/PTau/PPar/PRes/PRep case自己写（比较简单）
- POut/PIn case先写框架，x≠k的情况用set_none_insert_at_subst，x=k的情况用admit
- body部分先用admit，后续处理

### 步骤3：逐个修复admit
- x=k的case：证明T'=T
- body部分：证明上下文等价，或用其他策略
- PIn case：类似POut

### 步骤4：编译验证，确保0 Admitted

## 四、能力评估

### 我有能力完成吗？
- set_none_insert_at_subst引理：可以验证DeepSeek的证明，或自己重写
- substitution_general的简单case（PVar/PZero/PTau/PPar/PRes/PRep）：可以自己写
- POut/PIn的x≠k case：有了set_none_insert_at_subst后应该可以完成
- x=k的case：需要仔细分析insert_at的定义，应该可以完成
- body部分：最复杂，可能需要新策略或辅助引理

### 需要什么资源？
- 不需要额外资源，只需要仔细写证明

### 需要S01或S00配合吗？
- 如果body部分的上下文等价问题无法解决，可以派任务给S01做哲学研判
- 但目前先自己尝试

## 五、执行规划

1. 验证set_none_insert_at_subst引理的证明，添加到Layer2.v
2. 写substitution_general的证明框架，简单case自己完成
3. POut/PIn的x≠k case用set_none_insert_at_subst完成
4. x=k的case仔细处理（证明T'=T）
5. body部分尝试用IHQ，如卡住则记录阻塞派给S01
6. 编译验证，确保0 Admitted
7. commit+push

---
**记录者**：S04 Coq形式化分站（明旭）
**时间**：2026-09-02 11:30
