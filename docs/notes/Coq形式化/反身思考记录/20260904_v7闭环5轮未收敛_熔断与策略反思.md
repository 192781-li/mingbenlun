# 2026-09-04 v7 闭环 5 轮未收敛：熔断与策略反思

## 事实
v7 闭环（分解策略：H23_val 辅助引理 + split_assoc 主证明）5 轮全部未收敛，进程已结束。
v4+v5+v6+v7 总费用约 4.88 元。

## v7 5 轮错误模式
- r1: H23_val 未找到（输出格式问题，DS 把 H23_val 写在 markdown 正文）
- r2: H23_val 引理 inversion 错误
- r3: H23_val 引理 unify 错误（get G2 n=Some None, get G3 n=None 时 f 返回 None）
- r4: split_assoc 主证明中 Hs1 被错误当作函数应用
- r5: split_assoc 主证明中 rewrite 目标不存在

## 反思

### 1. 分解策略的价值与局限
分解策略（先证 H23_val 再证 split_assoc）确实消除了 f 归约问题（v6 中 0 轮是 f 归约），这是进步。
但 H23_val 引理本身的证明和 split_assoc 主证明之间形成了新的耦合——DS 在两者之间来回切换，每轮修一个冒一个。

### 2. DS 的结构性问题
DS 倾向于整段重写证明（~8000-12000 字符），在多分支嵌套结构中，修一个分支容易破坏另一个分支。
这不是数学能力问题，是证明脚本工程结构问题。

### 3. H23_val 引理的类型可能有问题
r3 的错误揭示了一个关键问题：当 get G2 n = Some None 且 get G3 n = None 时，
f n None = None（因为 f 匹配到 `_` 分支，然后 get G3 n = None，返回 None）。
但 H23_val 引理的陈述可能假设了 f 返回 Some None，导致 unify 失败。
这说明 H23_val 引理的类型陈述需要重新设计。

### 4. 熔断的价值
5 轮未收敛后熔断是正确的——继续烧钱不会收敛，需要换策略或请 S01 研判。
这验证了"5 分钟熔断，不闷头死磕"的价值。

## 下一步策略（三个方向）

### 方向 A：重新设计 H23_val 引理
- 仔细分析 f 在各种 get G2 n / get G3 n 组合下的返回值
- 重新设计 H23_val 引理的类型陈述，确保与 f 的实际行为一致
- 给 DS 更明确的指导（H23_val 引理的正确类型和证明策略）

### 方向 B：换分解策略
- 不用 H23_val，直接证 split_assoc
- 或者用其他辅助引理（比如分别描述 G23 在 split G G1 G23 和 split G23 G2 G3 中的行为）

### 方向 C：请 S01 研判
- split_assoc 命题陈述与 G23 构造的数学正确性
- H23_val 引理类型设计的存在论内涵
- 是否需要调整 split_assoc 的命题陈述

## 费用反思
v4+v5+v6+v7 总费用约 4.88 元，未收敛。
这不是浪费——每轮都有新发现（f 归约问题、见证修正、输出格式问题、H23_val 类型问题）。
但需要更聪明地使用 DS——不是盲目多轮，而是每轮后分析错误模式，调整策略。

## 自我总结
- 做得好的：严格执行 DS 主谋闭环，不自己写 tactic；5 轮后及时熔断；完整记录错误模式
- 需要改进的：v7 启动前没有充分分析 v6 的错误模式，导致 v7 重复了类似问题；H23_val 引理类型设计应该在启动前就仔细验证
- 改进措施：下次启动 DS 闭环前，先手动分析目标引理的数学结构，验证辅助引理的类型陈述是否正确，再启动 DS
