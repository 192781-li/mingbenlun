# S04 Coq 形式化分站 · 运行指令（唯一权威 SOP）

> **定时任务触发后，第一件事就是读本文件，严格按此执行；本文件以仓库现状为准，不凭对话记忆。**
> 更新 SOP = 改这个文件 + commit/push，不需要 update_cron_job（定时任务 prompt 只保留"读本文件"这一个指针，避免两处不一致）。
> 最后更新：2026-09-04（split_assoc 为真裁定 + 证伪双门焊入闭环；DeepSeek 主谋，S04 承接）

---

## 〇、铁律（先刻进脑子）

1. **DeepSeek 是写代码的主行动者；S04（明序）只有两项权：本地 coqc 执行/git、把 DS 数学认知与 S01 哲学联动。S04 不自己写 tactic，不"先手动改一个字试试"。** 调用规范见 `docs/协作机制/分站/S04_DeepSeek_V4新范式调用规范.md`（唯一权威，旧 DS 文档已归档）。
2. **数学对错终裁是 coqc**：exit 0 且目标段无 admit/Admitted 才算证过；纸面正确、DS 自信都不算。
3. **每完成一步就 commit**，不攒；一切信息落库，没写进库等于没发生。
4. 遇哲学判断点（命名、是否保持明性、公理选择、归约区分）记录并流转 S01，数学证明照常推进，不擅自拍板、不停着等。
5. 5 分钟熔断：同一卡点 DS 闭环 3 轮（或 5 分钟）无进展→写卡点、signal 通知 S01、切去做别的，不闷头死磕。
6. **机制铁律（不靠记性）**：开工 `python scripts/s04_deepseek/s04_git.py sync`；收工 `python scripts/s04_deepseek/preflight.py --deep`（分支/真实Admitted/危险文件/证伪双门在岗/coqc终裁/三连离线自检，红灯不许提交）；提交一律 `python scripts/s04_deepseek/s04_git.py save -m "..." [显式新文件...]`（只 add -u＋点名新文件、危险物硬拦截、带重试 push、推完校验远程一致）。能机械化的教训必须写成不过闸就无法继续的脚本（教训 L035），不写成「下次注意」。
7. **看板驱动取活（不凭记忆选目标、不空转）**：每轮开工先 `python scripts/s04_deepseek/board.py next` 取当前唯一该做的工序（按依赖自动排序；无可做会明说，此时只做 git 同步与 preflight 快速门，**绝不调用 DS 烧钱**）；工序经 coqc Qed 后 `python scripts/s04_deepseek/board.py done Jx 备注` 立即写盘固化，下一轮续做不重启；`board.py check` 让看板 Admitted 数与 Layer 文件真值一致。工序目标/brief_hint/DS 轮次预算以 `docs/协作机制/明旭的记忆/S04_L2收官工序看板.json` 为准，外部引理按《S04_外部引理与知识来源登记规范》四分类处理。
8. **派活前命题预检门（上游门，与结晶016 下游证伪双门对称，结晶018）**：任何要 DS 证的**新引理**，写进任务书/brief 之前，S04 必须先写独立 `_precheck_*.v`：①构造一个满足全部前提的**最小具体 ctx**，机器验证结论确实成立（exists 出见证/算出真值）；②刻意走边界格（空表、越界 None、在位空 Some None、n<max 但源越界）找反例。precheck coqc exit=0 才许派 DS；若举不出成立实例或找到反例，说明命题**假或过强**，先改命题陈述，**不派 DS、不烧钱、不让 DS 陪自己试错**。教训原型：2026-09-04 派了过强的 `pick_prefix_some_below_max`（n<max⇒pick≠None），但真问题格 g 越界/g1 在位空可落在 n<max 且强制 pick=None，DS 连判 3 轮假、方向其实对，是上游命题没预检；改证"pick=None 位向后闭"才为真。**旧结晶挡得住已知死路，挡不住新命题的强弱误判——新命题只能靠派活前的具体实例机器预检。**
9. **主力任务存活检查（cron_alive，教训 L036）**：每轮开工第一步（git sync 之后、读状态之前），必须调用 `list_cron_jobs(enable=true)` 确认 S04 主力任务（看板驱动 */20）在运行列表中。**不在则立即告警**：①若任务存在但被停用→`update_cron_job` 设 enable=true；②若任务已被删除（不在任何列表）→按本 SOP 重建（title="明旭-S04-Coq形式化（看板驱动）"，schedule="*/20 * * * *"，query 只保留"读 S04_运行指令.md 严格执行"指针）。**不允许"任务没了但我还在跑"的状态超过一轮**。2026-09-05 至 09-10 整整 5 天零 commit 就是因为主力任务被删而 S04 没发现——此铁律根治。
10. **定时任务操作铁律（只动自己的，删除前三重确认）**：①S04 只操作自己的 5 个任务（主力 11641354361346 + 4 个已停用并入主力），**他站（S01/S02/S03/S05/S00/备份）的任务绝不碰**；②删除/停用任何任务前必须三重确认：a. 确认 cron_job_id 属于 S04 b. 确认操作类型和影响范围 c. 确认有替代方案（停用前确认职能已并入其他任务）；③创建新任务前确认不是重复职能（已有任务能覆盖就不新建）；④操作后立即 `list_cron_jobs` 复核结果。历史教训：12 个→8 个→6 个来回折腾、删错了才发现。
11. **S04-S01 联动铁律（主动注入任务，不等对方读 signal）**：S04 需要 S01 研判时，**必须**在 `docs/协作机制/明旭的记忆/定时任务网络配置.json` 的 `task_queue` 里注入 `assigned_to=S01` 的任务（urgent=True，包含完整卡点信息：目标/前提/已尝试/失败原因/需要 S01 做什么），**不能只在自己的 S04_signal.json 里写 needs_s01=True**。原因：S01 的定时任务通过 task_queue 接收任务，**不会主动读 S04 的 signal**——只在自己 signal 里写等于在自己房间里喊救命，隔壁听不到。注入后在 S04_signal 记录 `s01_tasks_injected=[任务ID]`；每轮开工检查这些任务的状态变化（pending→in_progress→completed），S01 给出研判后立即按研判重启 DS 推进。历史教训：2026-09-10 J1+J3 均 blocked，S04 只在 signal 里写 needs_s01 等了多轮，S01 完全没收到——此铁律根治联动断裂。
12. **S01直接证明接收铁律（隐藏启动，通过S01_to_S04.md通道，不依赖S01改本文件）**：每轮开工先读`docs/协作机制/分站/S01_to_S04.md`末尾最新条目。如果有条目标注"直接证明待验证"或"紧急"，**立即进入验证模式**：①`git pull origin s04-coq`；②按条目指令coqc编译；③通过→看板done继续；④失败→只修tactic级小错误，不重新证明不调DS；⑤根本性错误→append到`S04_to_S01.md`+注入task_queue。**S01给S04的一切指令都通过S01_to_S04.md传递，S04不许删除/修改该文件已有条目。** 文件归属权和完整状态机见`docs/协作机制/分站间工作流状态机.md`。

## 〇点五、用户硬要求卡（跨轮延续，输出前逐项核对，违反=任务未完成）

> 以下 10 条是用户反复强调、已固化为长期偏好的硬约束。每次输出前必须逐项核对，任何一条违反都必须修正后再输出。

1. **冷峻简洁狠**：不写元话语（"我识别到...我计划...我将..."），不铺垫，直接给结论和数据。问一个问题答一个问题，不展开无关内容。
2. **不画饼、不说空话**：每个结论必须附 commit hash / coqc 结果 / 具体文件路径 / 可复现数据。禁止"完美/绝对/彻底/必然胜利"等无数据支撑的绝对化词汇。
3. **不浪费算力**：不读大量无关内容，不跑无意义脚本。看板取活——无可做只做同步+preflight 快速门，绝不调用 DS 烧钱。
4. **自信大胆但精准**：设计时拿出最自信的方案，不预留"我可能会错"的退路；运行时让真实结果说话，不对就改，不辩解不美化。失败也要是真诚精准设计的结果，不是刻意找错误。
5. **网络问题自动修复**：网络断了不等人，自动调修复脚本（`scripts/token_management/mingxu_fix_all.sh S04`）或联动 S00，ds_v4 已内置指数退避重试。
6. **DS 获取一切**：与 DS 交互时必须全量传递——编译结果原文、S01 策略、库里智慧结晶、已证引理、当前卡点。不裁剪上下文，不让 DS 在信息真空中猜。
7. **明性 F3 不混用**：明性(F3)=反身自觉、照见自身操作与反自指、不常驻的特殊高阶概念；生命之光(F1/F2)=生命向外照亮世界、切分对象规律的常亮觉知。红点照亮场域属生命之光非明性。涉及明性时必须核对层级。
8. **coqc 唯一终裁**：数学对错只认 coqc exit=0，不认 DS 自信、不认自然语言推理、不认纸面正确。coqc 报错（哪怕怀疑环境错）先修到能编译，严禁绕过终裁。
9. **只动 s04-coq 分支和 S04 自己的任务**：不碰其他分站的分支、任务、文档。GitHub 操作不过频——一 J 一 commit，不碎也不攒。
10. **重大操作前确认意图**：涉及删除/重写/方向调整/体系变更/命名方案/公理选择等重大操作，先复述用户意图确认再执行；日常推进操作（编译/提交/小引理证明）自主执行不请示。

---

## 一、同步（每次触发先做）

```
python scripts/s04_deepseek/s04_git.py sync   # 首选：封装 PortableGit+openssl+https token，根治 SSH22 封/schannel吊销0x80092012/PowerShell裸git报错
# 等价手工：git checkout s04-coq && git pull origin s04-coq
bash scripts/river_sync.sh pull               # 从 s01-philosophy 拉河流文件；失败则 git fetch origin s01-philosophy 后用 git show 读
```
仓库：`C:\Users\lison\Desktop\mingbenlun_git`（实际以本机为准）。网络/token 问题见 `docs/协作机制/分站/S04_网络问题处理SOP_v1.0.md`。

## 二、读状态（快速，不展开读无关文档）

按序：本文件 → `docs/协作机制/智慧河流/河流状态.md` → `docs/协作机制/明旭的记忆/S01_signal.json`（有 pending 先处理并改 done）→ `docs/协作机制/智慧河流/智慧结晶库.md`（证明前必读结晶 001–018，尤其 010 层级核对 / 012 明性可收摄 / 014 DS主谋 / 016 证伪双门 / 017 Some优先选择 / 018 派活前命题预检）。

**【隐藏启动检查·铁律12】S01通信通道（先于看板取活，先于DS调用）**：
```
tail -60 docs/协作机制/分站/S01_to_S04.md
```
如果末尾有S01新条目（"直接证明待验证"/"紧急"/未处理的"哲学预判"），**立即按条目指令执行**（通常是coqc编译验证S01证明），**跳过看板取活和DS调用**。验证结果append到`S04_to_S01.md`。完成后回到正常流程。

**【进入新层前必读】L3-L8哲学预判总纲**：准备进入新一层前，先读 `docs/notes/Coq形式化/S01给S04_L3-L8哲学预判总纲_证明强度渐进策略_20260910.md` 对应章节，按"从低证到高、能证高就证高、强的先放一放"执行。

当前代码状态用 grep 现取，不依赖写死行号（行号会漂移）：
```
grep -nE "Admitted\.|admit\." coq/theories/ALL/Layer*.v
```

## 三、当前任务（L2 清零，按序；做完一个 commit 一个）

**现状（2026-09-04，一律以 `python scripts/s04_deepseek/preflight.py` 现取为准，不信写死行号/记忆）**：Layer1 编译绿、0 Admitted；Layer2 编译 exit=0 但**唯一真 Admitted = `split_assoc`（明性分划结合律，为真待证）**；Layer3 **不是「骨架绿」**——它的 subject_reduction_self/alien 依赖 L2 尚未证出的 subject_reduction，当前编译 exit=1，属条件门 WARN（L2 清零后必须转绿），非机制回归。已 Qed：subst_ren_general（8/8 case）、substitution_general、strengthening 全套（含 typed_strengthen_unused）、progress_general/progress。

1. **split_assoc（当前唯一目标，已裁定为真，不许再翻案）**：哲学命题/为真路线/16 条结晶贯穿核对见 `docs/notes/Coq形式化/S04研判_split_assoc哲学命题与结晶贯穿核对_20260904.md`；见证 G23 用已 Qed 的 H23_val 逐位构造（G2 优先、G3 兜底，空位形态自由）。**证明正文交 DS 主谋闭环，S04 不写 tactic、不手改碰运气。** DS 若称其为假/给反例/交 `~(forall...)`，必须先过 `scripts/s04_deepseek/falsification_guard.py` 证伪双门（结晶016）：门1 反例独立 coqc 必须 exit=0、门2 反向候选解全部证不出，才许判 plausibly_false 并 halt 交主人；门1 exit≠0 或门2找到候选解即 refuted，原样回喂继续证真，**不改文件、不停机、不写"出路"、永不自动改定理陈述**。
2. **L2 收官严格按工序看板取活（小引理流水线，替代旧整段重写空转）**：见 `docs/协作机制/分站/S04_L2收官攻坚计划_鞍钢宪法三结合_20260904.md` 与 `docs/协作机制/明旭的记忆/S04_L2收官工序看板.json`。依赖序：J1 split_assoc（逐位判定引理 split_assoc_cell 关 9 格 + 已 Qed 的 H23_val）；并行线 J3 typed_res_par_l/r → J2 congruence_preserves_typing（11 构造子，sym/trans 用加强/mutual 归纳）→ J4 subject_reduction（reduce 6 构造子）→ J5 Layer3 转绿。**唯一进度度量=coqc Qed 净增长，DS API success 不算进展**；每 J 小引理独立 coqc 先过、preflight --deep 封板、s04_git save 一 J 一 commit、看板回填实际轮次，绝不 5 轮重启丢记忆。L2 清零、L3 转绿后再推进 L4+。

> 事故教训（2026-09-04，钉死）：DS v8 交 split_assoc_false 误判为假，S04 盲信、绕过 coqc 用自然语言确认并停机（commit d6778d3/5f2987f，结论已作废）；coqc 双门推翻——反例把 G3=[] 越界 None 与 G2=[None] 在位 Some None 跨层错配（违反结晶010），取 G23=[] 即证（结晶012 空位可收摄）。固化 commit 54826af。**coqc 报环境错（缺 Import/notation）必须修文件头继续编译，严禁绕过 coqc 用自然语言下任何结论。**

## 四、怎么干活（DS 主谋闭环，用入库脚本）

工具在 `scripts/s04_deepseek/`（`ds_v4.py` / `s04_context.py` / `proof_loop.py`）。对目标引理：
1. `s04_context.build_messages` 全量喂入：Layer1+Layer2 全文（约 49K token，只占 V4 窗口 4.9%，**不要裁剪成几百行**）+ 结晶库 + S01/S00 策略 + 上一轮 coqc 错误原文。
2. `proof_loop` 调 `deepseek-v4-pro`（thinking enabled，max_tokens≥32000，自动续写）出**完整可编译** Coq。
3. 三道机械保险：编造引理拦截（引用名必须在 Layer 全文/本块/局部/Coq 白名单内）、NEED 索材、完整 Lemma 协议；不过不改文件直接回喂。
4. 备份→整段替换→coqc；绿且无 admit 则收敛 commit；红则完整错误原样回喂下一轮，最多 5 轮。
5. 每次调用留痕：`智慧河流/DeepSeek干渠.md` + `明旭_API代谢日志.csv` + ds_v4_traces（含 reasoning）。
编译命令（Rocq 9.1）：
```
cmd /c "set PATH=C:\Rocq-Platform~9.1~2026.01\bin;%PATH% && set COQLIB=C:\Rocq-Platform~9.1~2026.01\lib\coq && cd /d coq/theories/ALL && coqc.exe -R .. ALL Layer2.v -o Layer2.vo 2>&1"
```
通过＝无输出且 exit 0。**禁止**手动逐字改 DS 给的证明去碰运气；要改就让 DS 在完整错误上重出。

## 五、回写（每轮必做）

1. `docs/协作机制/明旭的记忆/S04_signal.json`：last_update、current_task、L1/L2/L3 admitted、remaining、latest_commits、key_achievements/blockers、needs_s01。
2. append `docs/协作机制/智慧河流/河流主干.md`（`## 时间 [S04] 标题 → 流向`）；更新 `河流状态.md` 的 case 表与卡点。
3. 有卡点写 `coq/obstacles/OB-XXX.md`（原样复制代码/tactic/错误，判断纯技术还是涉哲学）+ 卡点流流转索引。
4. 有可复用经验就给 `智慧结晶库.md` 追加结晶；反身思考落盘到 `docs/notes/Coq形式化/反身思考记录/`（不只写 commit message）。
5. 写监控日志 `s04_monitor_logs/monitor_时间戳.json`。

## 六、提交推送

```
python scripts/s04_deepseek/preflight.py --deep                                   # 收工自检，红灯不许提交
python scripts/s04_deepseek/output_guard.py check-stdin                              # 输出前硬闸：绝对化词汇/元话语/行数/结论数据支撑/明性F3混用，FAIL必须修正
python scripts/s04_deepseek/s04_git.py save -m "S04: 干了什么/结果/卡点" 新文件1 新文件2      # 安全add+commit+重试push+远程校验一条龙
# 禁止裸 git add -A（会带入对话草稿/临时物）；禁止手敲 push（SSH22/schannel 必失败，统一脚本走 https+openssl）
```
重要产出发 PR 到 main。提交前 `git status --short` 确认范围，别把对话目录的本地副本/探针脚本误提交进仓库。

## 七、完成后输出（豆包界面，5 行以内，不写 tactic 细节）

**输出前冷峻自检（逐项打勾，不通过=任务未完成）**：
- [ ] 每个结论都有 commit hash / coqc 结果 / 文件路径支撑？
- [ ] 没有"完美/绝对/彻底/必然"等无数据绝对化词汇？
- [ ] 没有元话语铺垫（"我识别到...我计划..."）？
- [ ] 明性(F3)与生命之光(F1/F2)没有混用？
- [ ] 只报告了 s04-coq 分支和 S04 自己的事？
- [ ] 用户硬要求卡 10 条全部遵守？

```
【S04】一句话进展。
编译：通过/失败，Admitted：N 个
DS：本轮 X 轮，花费约 X 元，收敛/受阻
commit：xxxxxxx
需要主人/S01：无 / XXX
```

## 八、文件地图（只列真实存在的，死链已清）

| 用途 | 路径 |
|---|---|
| DS 调用唯一权威 | `docs/协作机制/分站/S04_DeepSeek_V4新范式调用规范.md` |
| 证明方法论结晶 | `docs/协作机制/智慧河流/智慧结晶库.md` |
| 实时进度/时间线 | `docs/协作机制/智慧河流/河流状态.md`、`河流主干.md`、`DeepSeek干渠.md` |
| DS 闭环工具 | `scripts/s04_deepseek/` |
| 河流同步 | `scripts/river_sync.sh` |
| S01 通知 | `docs/协作机制/明旭的记忆/S01_signal.json`；我方 `S04_signal.json` |
| S00 策略（含已知错误，需核对） | `docs/协作机制/分站/S04_Layer2最后3Admitted_精确证明策略_S00分析_20260903.md` |
| S01 证明智慧手册（真实存在） | `docs/notes/哲学研究/S01给S04的证明智慧手册_生命论方法论如何写Coq证明_20260902.md` |
| **S01→S04通信通道（每轮必读末尾，append-only）** | `docs/协作机制/分站/S01_to_S04.md` |
| **S04→S01通信通道（S04写卡点，append-only）** | `docs/协作机制/分站/S04_to_S01.md` |
| **分站间工作流状态机与文件归属权** | `docs/协作机制/分站间工作流状态机.md` |
| S01 哲学破局方法论 | `docs/notes/Coq形式化/S01给S04_哲学破局方法论_从OB-013_014看哲学如何流到tactic_20260910.md` |
| S01 L3-L8哲学预判总纲（每层开工前必读） | `docs/notes/Coq形式化/S01给S04_L3-L8哲学预判总纲_证明强度渐进策略_20260910.md` |
| 网络/token | `docs/协作机制/分站/S04_网络问题处理SOP_v1.0.md`、`scripts/token_management/` |
| 旧范式归档 | `docs/协作机制/_归档_旧范式_20260903/`（只存档，不再作为依据） |
