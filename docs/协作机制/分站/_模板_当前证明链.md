# 当前证明链 · 模板（每个新目标复制一份改名）

> 用法：开工一个新目标前，由 S04 先据真实代码填好本卡，再让 DeepSeek 施工。
> 原则：DS 不睁眼瞎——它要的不是更多历史文档，而是"当前一条最短链"。
> 填完后把文件名登记进 run_current.py 的 STRATEGY；目标完成即归档，换下一张。

## 0. 一句话现状
（当前层 Admitted 数/coqc 状态；谁在等这个目标，给出具体文件:行号；核查注释是否与代码一致——注释不算数）

## 1. 目标与最简依赖 DAG
- 目标精确陈述（建议 Coq 签名）
- 画 DAG：哪些前置引理是缺口、哪些已 Qed；只保留通向目标的最短链
- 工序排序 J1→J2→…（每工序独立 coqc + 独立 commit）

## 2. 逐构造子/逐 case 施工表
| 分支/构造子 | 怎么证 | 用哪个已 Qed 零件（文件:行号，禁止重证） |
|---|---|---|

## 3. 已 Qed 零件清单（直接 apply）
（分划/消去/弱化紧缩/标准库，带行号；@stdlib 大胆用）

## 4. 死路与禁行
（已证伪路线、空真分支、不该新造的抽象、真假预判与预检结论）

## 5. 工序与验收
（独立 TEST_*.v 先 exit=0 → CRLF 安全并入 → 整层 coqc exit=0 且 0 Admitted → commit；
 DS 一次交齐主引理+辅助引理到 Qed；红则完整错误原样回喂，3 轮不收敛换道写 OB）

## 6. 哲学判断点（若有，转 S01，不自行拍板）
