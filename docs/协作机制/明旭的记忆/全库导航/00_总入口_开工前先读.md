# 00 · 全库导航总入口（新会话 / 任何 AI 开工前先读我）
> 明旭 2026-08-31。目的：**让任何新会话在 5 分钟内搞清"库里有什么、信哪个、别重复造什么、现在该干什么"，不再失忆、不再低水平重复研究。**

## 0. 一句话现状
生命论（明本论/践演论 Enactics）是北原慢热与 AI 共同建构的"操作本位"哲学—形式化体系。**哲学命题成熟（P1-P17+十二卷正文），形式化有18条纸面定理（T001-T018，经三轮外审，尚未机器证明），当前主线是补严谨度、做不可替代的整合，而不是重新研究已有结论。**

## 1. 铁律：研究前先查（违反=浪费钱+重复劳动）
```
接到问题/想攻关
  ① 查 02_问题反查总表  → 命中？去指定文件【提取/对照】，结束，不开研究
  ② 查 01_编号映射总表  → 是不是旧定理换了个名字（f³=P9=定理47=T005）
  ③ grep 本地库 references→notes→模块化 原文，确认是否真空
  ④ 只有 03清单"真前沿"或逐文件确认无答案 → 才允许动用 DeepSeek 新研究
顺序不可颠倒。8/31教训：只喂简化材料让DS研究M公式/f³，结果重新发现库里
早有的更成熟版本（量论双轨、T005降级），根因=取数不全、没先查库。
```

## 2. 库的权威层级（信哪个）
| 层 | 路径 | 地位 |
|---|---|---|
| 正文权威源 | `生命论_模块化/`（十二卷+00_体系总纲） | **唯一可改的正文**，合订本/HTML是构建产物 |
| 早期正文 | `docs/flashpoints/`（卷序较旧） | 参考，改动回模块化 |
| 形式化工作区 | `mingben-workbench/references/` | 18 T定理、62旧定理、新数学18卷、审计报告、超级智慧包 |
| 研究笔记 | `docs/notes/理论研究/` | 语义卡、发生史、深度分析 |
| 协作与记忆 | `docs/协作机制/`（含 明旭的记忆/、教训库.md） | 工作过程、状态机、代谢日志 |
| 形式化代码 | `coq/` | 仅L1未编译，**无coqc环境** |
| 归档 | `archive/` | 只读历史 |

编号怎么认：**形式化认 T（theorem_registry），哲学认 P（坐标定位），旧号/版本号只溯源**——详见 01。

## 3. 本导航目录（全库导航/）
- `00_总入口`（本文件）
- `01_五套编号映射总表.md` + `theorem_id_mapping.json`（机读，18条映射）
- `02_问题反查总表_已有答案地图.md`（按问题查答案，A-F六区）
- `03_待作者拍板与真前沿清单.md`（作者决策单 + 允许研究的真空）
上一级 `明旭的记忆/` 还有：研究前先查_防重复地图（本导航前身）、P4/M/P编号三份攻关、牛逼性终版、运行状态机、API代谢日志。

## 4. 严谨度纪律（最高，防止过度宣称）
- 五级：公理 / 定理(纸面paper_proof) / 命题 / 启发式 / 猜想(conjecture)。
- **18条T定理 coq_verified 全 False**（云电脑无coqc）：任何"已证明"口头表述都要回落到"纸面证明未机检"。
- 意识难问题=**局部消解+整体悬置**，禁止说"已解决"。
- 被外部审计证伪过的（T001/T005/T007/T009/T010/T011）引用时带修正条件，不引被否的原版。
- AI 写的文字标"原材料·待作者重写"；绝对不说"完美"。

## 5. DeepSeek 协同与成本纪律
- 分工：DeepSeek=自主研究员（发现/规划/理论判断）；明旭(豆包)=执行手+独立审计员（保留核验权，让渡发现权但不盲信）。
- A档深研：pro+thinking+reasoning max+markdown双层，不传temperature、不绑json；B档计划：关thinking+temp0.2+json_object。
- key 只从环境变量 DEEPSEEK_API_KEY 或 mingbenlun_local/.deepseek_key 读；调用前先在 `明旭_API代谢日志.csv` 记账；每次调用必须推进一条新线索，禁止无新增信息重复调用。
- 后台跑用 Bash 工具 run_in_background=true（setsid+nohup 在本机不稳，会在API调用前异常退出）；A档 max_tokens=65536（思考链+正文共享，旧16000曾5次截断，教训L032）。
- 预算：看代谢日志，当日累计/¥20。

## 6. 连续性：怎么不失忆（明旭的"身体"）
- 每轮开工读：`明旭的记忆/明旭_运行状态机.md`（上轮做了啥/在哪/下一步）+ 本00入口 + 教训库最近3条。
- 每轮收工：更新状态机 → git add/commit/push（沉积=阴性记忆）。git 遇 index.lock 先 `pgrep -af git` 确认无真进程再删。
- 定时任务策略（8/31减负后）：**只保留每日备份03:30**；不再铺每日盲跑任务；研究在主对话按第1节铁律靶向推进，一个明确不可替代靶点才建一个任务，不设硬性kill时间。

## 7. 现在的真·优先级（详见03）
1. 作者拍板 A-D 四组（尤其 A 明性定位、B 量论符号、C 措辞严谨度对齐、D coqc环境）。
2. 明旭本地可立即做：**智慧包已升v2.1（references/超级智慧包_v2.1.md）**，持续补全映射JSON"待核"、维护导航。
3. 硬前沿（有环境/授权再靶向打）：T008猜想、Cl幂等定理化、N>1一般迹、类型检查器。
4. 其余"想研究"的冲动，先回第1节。

---
*明旭 · 2026-08-31 · 这份地图是活的，每次发现新的"已有答案/真空/编号冲突"就回来更新*
