# S04 启动必读 · 一页纸

> 每次触发先读这一页；完整流程以 `docs/协作机制/分站/S04_运行指令.md`（唯一权威 SOP）为准。
> DS 怎么用以 `docs/协作机制/分站/S04_DeepSeek_V4新范式调用规范.md` 为准。最后更新 2026-09-03。

## 一、你在干什么（一句话）
证 `subst_ren_general` / `substitution_general`——**同一类型的操作权可从一个位置挪到另一位置，进程只认类型不认位置**（代换保类型，类型系统基石）。

## 二、当前进度（行号会漂移，以 `grep -nE "Admitted\.|admit\." Layer*.v` 现取为准）
- Layer1：0 Admitted，绿。Layer3：0 Admitted 骨架，绿。
- Layer2 剩 **3 个目标**：
  1. `subst_ren_general` 的 **PPar**（对进程归纳，7/8 已 Qed，仅并行组合）——strengthening 紧缩碰撞位冗余后仿 Layer1 `ren_typed` 的 ty_par（结晶 011/012，工具引理已全 Qed）。
  2. `substitution_general` 的 **PPar**——1 通后用 `subst_var_eq_ren` 一行导出。
  3. `congruence_preserves_typing`（整体 Admitted，11 构造子，参考 S00 策略文档但"m≥k 单射"为假须核对）。

## 三、操作权流动视角（写证明前想清楚）
- 上下文=操作权分布：Some T 有权、None 已消耗；use=操作权消耗（get 前置、set_none 后置）。
- 代换=操作角色转移，不是语法替换；绑定器是新世界入口，坐标整体后移（S m,S k）。
- 卡住就问：操作权在哪？从哪来？到哪去？中间经过什么？

## 四、智慧结晶（证明前必读，全文见智慧结晶库）
弱引理比强引理易证（001）｜重写比硬推有效（002）｜绑定器下参数同步偏移（003）｜从简到繁（004）｜证不出怀疑定义（005）｜5 分钟熔断（006）｜rewrite 代 subst（007）｜代换=非单射重命名先证一般引理（011）｜非单射+并行+weakening 需 strengthening 紧缩（012）。

## 五、DeepSeek 新范式（2026-09-03 起，旧"只给思路你翻译/只喂几百行/max 8192"全部作废）
- **DS 是写代码主谋，直接出完整可编译 Coq；S04 只做 coqc 验证、git、哲学桥梁，不自己写 tactic。**
- **全量喂**：Layer1+2 全文约 49K token，仅占 V4 1M 窗 4.9%，第二轮起缓存命中不到 1 分钱，不要裁剪。
- V4：`deepseek-v4-pro` 证明（thinking on，max_tokens≥32000，自动续写）；"空输出"是思考吃额度不是被掐断。
- 闭环：出码→编造引理拦截→备份→替换→coqc→错误**原样**回喂→绿且 0 admit 才收敛。工具 `scripts/s04_deepseek/`。

## 六、河流（硬约束）
`bash scripts/river_sync.sh pull` 开始、`push` 结束；干完更新河流主干/状态/S04_signal，卡点流转 S01。

**一句话**：同步→读状态→全量喂 DS 出完整证明→coqc 终裁→绿就 commit、红就原样回喂→回写河流。不自己硬扛，不裁剪上下文，不没验证说完成。
