# 河流状态（实时）

> 最后更新：2026-09-02（本轮） by S04
> 每次定时任务触发必须读本文件，干完必须更新

---

## 当前正在流动的卡点

### OB-009：n=m 线性资源边界 —— A线已落地，仅剩 PPar
- **状态**：主人拍板A线（加"目标位未被消耗"前提）。S04用数学把S01单参数谓词纠正为**双参数谓词 no_use_at_subst P m k**（与subst_var逐行镜像，统一排除 x=k / x=m(m<k) / x=m+1(m≥k) 三种落m）。substitution_general 8 case 证其7，**只剩 PPar 一个内部admit**
- **当前位置**：→S01（确认双参数谓词存在论语义 + 基于双参数谓词更新PPar策略 + 预判reduction场景前提是否天然成立）
- **文件**：coq/obstacles/OB-009_PIn被代换位置等于use消耗位置_线性资源边界_20260902.md；反身思考 docs/notes/Coq形式化/反身思考记录/20260902_OB009_A线落地_双参数谓词纠正_7of8case.md

### PPar（最后一个case，新主卡点）
- insert_at 增一位后 split 两侧上下文都长1，m,k可能落在不同子侧；S01已有专门策略（remove_at/split_insert_at/subst_var_not_used/typed_no_var_at_none/insert_at_none_irrelevant 5辅助引理），但该策略基于旧单参数谓词，需S01按 no_use_at_subst 更新
- 文件：docs/notes/哲学研究/S01给S04_PPar_case完整证明策略_最后一个最难的case_20260902.md（待导出精读）

## substitution_general整体进度（A线·双参数谓词版）

| Case | 状态 | 备注 |
|------|------|------|
| PZero | ✅Qed | |
| PTau | ✅Qed | |
| PVar | ✅Qed | name_subst_general |
| PRep | ✅Qed | subst_var_empty |
| PRes | ✅Qed | 绑定器(S m)(S k) |
| PIn | ✅Qed | x=k/x=m/x-1=m三边界被no_use_at_subst前提统一矛盾排除 |
| POut | ✅Qed | S04自写：通道n的</k×值n0的</k共4子分支+落k矛盾，body穿两层set_none_neq |
| PPar | ⏳admit | 最后一个，待S01更新策略 |

- 定理新陈述：`typed(insert_at k T Gamma)Q -> get Gamma m=Some(Some T) -> no_use_at_subst Q m k=true -> typed Gamma(subst_var m k Q)`
- substitution_lemma(k=0)已同步加 `no_use_at_subst Q y 0` 前提，Qed
- Layer2 顶层Admitted：**2个**（substitution_general整体[仅因PPar]、congruence_preserves_typing），内部admit仅1；三层编译0错误
- set_none_insert_at三引理、subst_name三引理、subst_var_empty/no_res_from7引理：✅

## 下一次涌动

### S01应该做
1. 确认双参数谓词 no_use_at_subst P m k 的存在论表述与命名（"代换后m未被消耗"），它替代了有索引错位的单参数 not_used_channel_at Q m
2. 基于双参数谓词更新 PPar 完整策略（split两侧如何分解这份前提）
3. 预判 subject reduction 调用 substitution_lemma 时 `no_use_at_subst Q y 0` 在red_comm通信场景是否天然成立
4. congruence_preserves_typing 策略（"不含OB009影子"版）与新前提的衔接

### S04应该做
1. 导出并精读S01的PPar策略文档，按双参数谓词对齐，喂完整上下文给DeepSeek拿骨架、自己数学把关
2. PPar证通后 substitution_general 整体 Qed（内部admit清零）
3. 再攻 congruence_preserves_typing，然后 subject_reduction / progress
