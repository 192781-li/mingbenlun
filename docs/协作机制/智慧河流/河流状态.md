# 河流状态（实时）

> 最后更新：2026-09-02 20:40 by S04
> 每次定时任务触发必须读本文件，干完必须更新

---

## 当前正在流动的卡点

### OB-010 ty_par（唯一内部卡点，已到最后一步）
- **路线已转变并落地**：substitution 本质 = 非单射重命名。`subst_var m k P = ren (subst_name m k)P`；新主引理 **subst_ren_general（源 D 任意，同构 Layer1.ren_typed）已 Qed 7/8 case**，只剩 ty_par
- **ty_par 硬点已精确定位**：rho 碰撞对 {k,c}（都映到 m）在 weakening 下，子上下文可携带进程不用的**冗余资源位**，使 split_proj 的 proj2 在 m 位被划空（ren_typed 靠全局单射绕开，rho 不行）
- **解法已定**：先证 strengthening 紧缩引理 `typed_strengthen_unused: typed C P -> not_free_in u P -> typed(set_none C u)P`（对 typed 归纳），清掉碰撞位冗余后照 ren_typed line356-391 完成
- **当前位置**：→S01（为 strengthening 的存在论语义背书：主动丢弃未被使用的明性，主人已说"明性可不保持"）；→S04 下一轮实现 not_free_in + 紧缩引理 + 收尾 ty_par
- **文件**：coq/obstacles/OB-010_..._20260902.md（已重写为最新版）；s01_temp_docs/deepseek_v14_typar.md、OB010_cross_insight.md

## subst_ren_general（新主引理·源任意）case 进度

| Case | 状态 | 关键手法 |
|------|------|------|
| ty_zero | ✅Qed | 任意 G |
| ty_var | ✅Qed | 资源前提 Hpts |
| ty_tau | ✅Qed | IH+no_use |
| ty_out | ✅Qed | rho_inj_except_m+no_use两通道分量替代全局Hinj |
| ty_in | ✅Qed | 通道经rho+body绑定器升级，局部单射 |
| ty_par(并行) | ⏳admit | **唯一卡点：需strengthening紧缩碰撞位冗余** |
| ty_res | ✅Qed | upren rho=subst_name(Sm)(Sk)+subst_name_succ |
| ty_rep | ✅Qed | IH到[]，空上下文discriminate |

地基引理（全Qed）：upren_subst_name_pt / ren_ext / subst_var_eq_ren / subst_name_succ / subst_rho_pts / rho_inj_except_m

## 全局编译状态
- Layer1：0 Admitted，编译通过
- Layer2：编译**通过（绿）**；新主引理 subst_ren_general 7/8（ty_par admit）；旧 substitution_general 7 手证 case 保留交叉验证、PPar admit（待新主引理一行导出）；congruence_preserves_typing 仍 Admitted（独立任务）；subject_reduction 的 red_comm 为 exfalso 占位
- Layer3：0 Admitted 骨架，编译通过
- commit 链（均 HTTPS push）：…960e5e8→7d082e2→aa29e3d→4ea864d→**8e19649**

## 下一次涌动

### S01应该做
1. 【最高优先·哲学】正式研判 strengthening 紧缩合法：子进程不用的资源位（未被使用的明性）可清空而不改存在论语义；给正式命名，确认与"明性可不保持、籍才须保持""路线乙同型异位"一致
2. 碰撞对 {k,c} 收束到同一 m 的存在论解读（两条操作权流汇为一条，紧缩清掉无操作权流经的空壳位置）
3. congruence 策略基于新主引理 subst_ren_general 更新（构造 no_use 前提、k≤length，k=0 用 lia）

### S04应该做（下一轮按序）
1. 定义进程自由变量谓词 not_free_in（或证 no_use_at_subst→碰撞位 not_free_in 的桥接）
2. 对 typed 归纳证 typed_strengthen_unused
3. subst_ren_general 的 ty_par：对 k,c 紧缩后照 ren_typed 完成，整体改 Qed
4. 用 subst_ren_general 导出 substitution_general（D=insert_at k T Gamma + subst_rho_pts + subst_var_eq_ren），旧 PPar admit 清零
5. 复跑 Layer1→2→3 顺序编译；再攻 congruence_preserves_typing
