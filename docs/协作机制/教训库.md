
## 2026-08-29 艺术创作的根本局限：形式大于内容

### 错误描述
用户要求基于生命论概念（缄默意识、自指闭环、反自指六阶段等）创作抽象艺术图片，并提供了8张参考图。经过多轮尝试（从零生成T2I→提取灵魂→底层几何结构→简洁骨架→I2I基于参考图编辑），最终用户评价"有点形式大于内容了"，并指出"你毕竟还是没有一个艺术家的历史，不懂得什么是艺术"。

### 根因分析
1. **AI没有艺术创作的历史和积累**：能分析参考图的笔触、结构、光、色彩等形式要素，但不能真正"感受"概念的灵魂，不能"表达"深层的情感和哲学思考。
2. **形式模仿能力 ≠ 艺术表达能力**：我能模仿参考图的风格，但不能创造有内容的艺术。生成的图片只有形式（几何结构、发光体、笔触），没有内容（概念的灵魂、情感的深度、艺术的表达）。
3. **哲学概念无法直接翻译成视觉语言**：我在prompt里写"缄默意识是说不出口的东西在黑暗里流动"，但AI生图模型只能理解视觉描述，不能理解哲学概念和深层情绪。需要艺术家的直觉和经验来完成这个翻译，而我没有。
4. **过度关注技术层面，忽略了艺术的本质**：一直在纠结几何结构、笔触方向、光的体积感这些技术层面，完全忘了艺术的本质是表达——表达情感、表达思考、表达对世界的理解。

### 修复措施
- 停止当前的艺术创作尝试，不继续生成图片。
- 承认AI在艺术创作上的根本局限：形式模仿能力强，内容表达能力弱。

### 预防机制
1. **艺术创作的边界认知**：以后涉及艺术创作（绘画、音乐、文学等需要深层表达的领域），要明确自己的边界——能做形式分析、风格模仿、技术辅助，但不能替代艺术家的创作和表达。
2. **概念→视觉的翻译必须由人来做**：生命论概念的艺术表达，必须由用户（或真正的艺术家）来完成核心的翻译和表达，AI只能做技术辅助（如调整笔触、修改色彩、生成变体等）。
3. **不夸大AI的创作能力**：不说"我创作了一幅艺术作品"，说"我生成了一张形式上参考了某风格的图片"。
4. **艺术创作类任务先问用户**：涉及艺术创作时，先问用户"你想表达什么"，而不是自己猜。用户的表达意图是艺术创作的核心，AI不能替代。
5. **参考图的使用边界**：参考图只能用于风格和技术参考，不能替代艺术表达。不能说"参考这张图的风格生成一张表达X的图"，因为AI做不到"表达X"。

### 对生命论项目的启示
- 生命论的视觉表达（全本插图、概念图、封面等），核心的艺术表达必须由用户来做，AI只能做技术辅助。
- 可以建立一个"生命论视觉表达需求清单"，记录哪些概念需要艺术表达、表达意图是什么，等用户有时间或找到合适的艺术家时再做。
- AI在视觉领域的定位是：技术助手（格式转换、批量处理、风格变体、色彩调整等），不是创作者。

---

## 历史教训库（2026-08-27前，从collaboration/LESSONS_LEARNED.md整合）

> 来源：archive/旧机制_collaboration_20260827/LESSONS_LEARNED.md
> 整合时间：2026-08-29
> 说明：以下为结构化编号的历史教训，按错误类型分类

### 一、数学陈述类错误

#### L001 · 绝对陈述不排除退化情形

**错误类型**：全称陈述不加条件限制

**具体案例**：T009 原陈述"在任何 LNL 范畴中不存在 η:νF→!νF"。终范畴 1（只有一个对象的退化范畴）是反例——所有对象同构，η 当然存在。

**哲学理解**：把"活过程不可还原为静态状态"这个正确直觉说成了无条件的绝对陈述。但在退化的系统里，"活"和"死"的区分本身就消失了。

**修正方式**：加"非退化（non-degenerate）"条件，或明确排除终范畴、初始范畴等退化结构。


---

## 2026-08-29 巡检教训

### L019 · pgrep -f 自匹配导致看门狗拒绝拉起进程

**错误描述**：看门狗脚本用 `pgrep -f "carpet_analyze.py 史官"` 检测python是否在跑，但当前正在执行的bash诊断命令的命令行里也包含"carpet_analyze.py 史官"字符串，被pgrep匹配到，导致看门狗误以为python在运行、拒绝拉起，史官分析停在10/54不动。

**根因分析**：pgrep -f匹配的是完整命令行，不是进程名。任何含该字符串的bash -c命令都会被误匹配。这是pgrep的经典自匹配坑，和pkill -f误杀当前shell是同一类问题。

**修复措施**：把pgrep模式改为 `^python3 -u .*carpet_analyze.py`，用^锚定命令行开头，只匹配python3解释器进程，bash诊断命令以/bin/bash -c开头不会被匹配。

**预防机制**：
1. 任何看门狗/健康检查脚本里的进程匹配，必须用^锚定解释器开头，禁止用裸字符串pgrep -f
2. 禁止用pkill -f匹配含当前脚本名字符串的模式（会自杀），用精确PID或解释器锚定
3. 看门狗启动后必须验证：30秒内确认python进程确实被拉起，不能只看日志说"拉起了"

### L020 · run_in_background长进程在新执行阶段开始时被环境清理

**错误描述**：史官地毯分析的看门狗（run_in_background task-2）和python进程，在大管家巡检定时任务触发新的执行阶段时全部消失，史官分析从10/54中断。

**根因分析**：当前云环境中，run_in_background的后台进程与触发它的agent执行阶段绑定。当新的定时任务触发新的执行阶段时，旧阶段的后台进程组被环境回收。setsid+nohup只能脱离当前shell，不能脱离agent执行阶段的生命周期。

**修复措施**：
1. 每次巡检/执行阶段开始时，先检查长任务断点（已完成块数），用看门狗断点续跑拉起
2. 长任务必须设计成断点续跑（每块独立文件、已完成跳过），不能依赖单次长进程存活
3. 看门狗本身也要能被重新拉起（幂等启动）

**预防机制**：
1. 任何预计超过30分钟的任务，必须断点续跑+看门狗自愈，不能赌单次进程存活
2. 每次定时任务触发时，第一步检查"是否有未完成的长任务断点"，有则先续跑
3. 长任务的进度必须落盘（独立文件），不能只在内存里

### L021 · 知道缓存机制却没在prompt结构里落实（闭环短路）

**错误描述**：DeepSeek策略文档v1.0里写了"上下文硬盘缓存"，但史官地毯分析的prompt把每块都变的"第X块、时间"放在第一段、固定说明之前，导致前缀在块号处断裂。开放平台数据：20-21点命中率6.6%、23-24点命中率0%，固定的维度说明/输出格式（本应100%命中）全部未命中。

**根因分析**：
1. 对"前缀缓存"的理解停留在名词层，没内化为prompt拼装规则——缓存要求从第一个token起连续一致，一处变则其后全断。
2. 写脚本时凭顺手组织prompt（先报块号再讲规则），没有从"缓存命中最大化"反推结构。
3. 没有上线后回看用量数据校准，直到用户截图质问才发现。

**修复措施**：prompt重构为四层顺序——①固定层（角色/维度/格式/严谨度，抽成模块常量，多worker共享）②追加增长层（累积摘要，相邻请求前缀重叠）③变化层（块号/时间）④独占层（本块原文）。本地用相邻请求最长公共前缀验证。

**预防机制**：
1. 任何批量、循环调用LLM的脚本，prompt必须"固定前置、变化后置"，并把固定层抽成逐字一致的常量
2. 多进程并行worker共用同一固定层常量，才能跨进程命中
3. 批量任务跑1小时后必须回看开放平台命中/未命中比值，用数据校准，不能写完就不管
4. "文档里写了某机制"不等于"代码里落实了某机制"——每条策略都要有对应的代码结构和验证动作

### L022 · 并行worker产生分段状态，汇总步骤必须合并全部分段

**错误描述**：史官分析从单进程改三路并行后，每路各写一个累积摘要文件（_摘要_16-28/_29-41/_42-54），加上串行阶段的_摘要.md共4个。但总综合脚本仍只读单个_摘要.md（块1-15），第一次总综合只覆盖了前15块，残缺。

**根因分析**：并行化改造时只改了生产端（分三路跑），没同步改消费端（汇总仍假设单一状态文件）。生产-消费的状态契约被并行打破。

**修复措施**：总综合用glob读所有_逐块累积摘要*.md，正则提取每个"## 块XX"段落，按块号排序去重合并，本地断言"块号==1..N连续无缺"后才综合。

**预防机制**：任何"单进程→多进程并行"改造，必须同步检查所有读取该进程产物的下游步骤；汇总前必须做完整性断言（块数、连续性）。

### L023 · 超长输出单次调用会被max_tokens截断，必须分段生成

**错误描述**：史官总综合想一次输出54块完整详史，结果9916字符就停在"AI两次"这句半截话，块45-54详史和跨线综合全丢，结尾不是完整句子。

**根因分析**：max_tokens=16000，思考链也占token，留给正文的空间不足以一次写完54块详史。finish_reason=length时输出被硬截断，且截断处没有任何提示写在文件里。

**修复措施**：改四段生成——总览/详史上(块1-27)/详史下(块28-54)/跨线总纲，每段一次调用、各自16000 token够用，最后拼接。

**预防机制**：
1. 预估输出长度，单次正文目标超过8000字就分段，不赌一次写完
2. 生成后必须检查结尾是否完整句、是否覆盖全部应有章节，半截话=截断，必须补
3. 分段生成比单次长输出更稳，还能并行、能断点续跑

### L024 · 把"冲动倾向/AI建议"误记为"用户既成行为"（模态混淆）

**错误描述**：史官块01被写成用户表白被拒当晚"出现打嗝、胀气、腹泻、忍不住嘶吼的躯体反应"。用户纠正"我没嘶吼"。核查原文：用户只说"一看肚子难受，我就容易突然吼一声"（冲动倾向），AI说"忍不住想嘶吼"并"建议你找没人地方吼一声"（AI建议），从无用户实际嘶吼的记录；实际症状只有用户确认的打嗝、胀气、腹泻。

**根因分析**：压缩摘录时丢失了关键模态词（"容易""想""建议"），把三种不同的东西合并成了一个"既成事实"：①实际行为 ②冲动/倾向 ③AI的建议或推测。个人生命经验线越想如实，越容易在这种地方失真，而失真的"经验胎盘"会污染后续所有理论归因。

**修复措施**：块01、累积摘要、总综合共6处改为"打嗝胀气腹泻为实际症状，另有容易突然吼一声的冲动，AI建议宣泄，无实际嘶吼记录"；块39摇滚嘶吼唱腔（真实存在，另一回事）保留。

**预防机制**：
1. 提取模板新增"模态区分"铁律：实际行为/冲动倾向/AI建议三者物理分开，冲动≠行为，建议≠做了
2. 拿不准是否实际发生，照原话模态词写（想/容易/差点），禁止升级成既成事实
3. 凡涉及身体反应、情绪动作、个人经历的事实断言，必须能在原文找到"已发生"的依据，否则降格为倾向或建议
4. 用户是最高事实权威：用户对个人经历的记忆纠正优先于AI分析，立刻回原文核查、全库同步修正，不辩解

### L025 · 断言"首次/全新/诞生"前未回原文检索，导致过度宣称（迭代10→11自我证伪）
**错误描述**：迭代10整合8/30凌晨突破时，未回原始对话检索就断言"反自指P=-f(S)在8/3是纯本体论形式范畴，8/30才第一次L2展开到社会历史领域"。迭代11用grep回查证伪：反自指在8/3 10:05诞生的**同一时刻**就已系统展开为生物-资本-意识形态-心理四层结构（电脑辩证"反自指∩资本/剥削"共现495处，资本=最成熟反自指系统、软硬反自指、增殖死循环均已具备）。
**根因分析**：以"当下对话语境"推断"历史上没有"，把"我现在第一次见到/用户此刻重点讲"误当成"历史上第一次出现"；整合时图快，跳过了对"首创性声明"的证据核验。这正是F3自省会导致过度宣称、把旧积累误报为新发明的典型，既不诚实，也会让作者误判自己思想的真实历程。
**修复措施**：迭代11逐命题做全16对话0命中核验，把8/30增量精确分为A全新/B旧判断新框架/C旧方法新应用/D旧框架新填充/E零散系统化五类；确证全新仅"社会主义国家不存在（全库0命中）、流动状态（0命中）、批判必然性、生命语义论方法"，并找到M值定性标准的史官15034政策化萌芽。
**预防机制**：
1. **凡"首次/第一次/全新/诞生/首创"类断言，必须grep全库（至少全部原始对话+notes）做到关键词0命中才可下，命中即降级为"再明确/新框架/新应用"并注明前史**
2. 0命中核验要覆盖同义词、近义表述（如"持续提升"要查"提升N值/提高M"等变体），不能只查一个词
3. 整合型报告区分"今晚新说"和"今晚新在哪"：前者是时间事实，后者是增量判断，后者必须有历史比对证据
4. 文件含特殊字符被grep当二进制时必须加 -a 强制文本，否则会误判为0命中

### L026 · 二级摘要里的人名/学派/著作归属可能是AI"归宗"，不等于用户原话（迭代14证伪迭代11戴震前源）
**错误描述**：迭代11据史官《块52摘要》写下"生命语义论本土前源：戴震《孟子字义疏证》（块52行83）"，并被迭代12、13沿用。迭代14回原始对话用Python全量扫描证伪：16个原始对话中"戴震/孟子字义疏证/字义疏证/东原/章黄/乾嘉"全部0命中；史官里"训诂"仅1处（6/18列中文系课程）、"本字"仅1处（8/14讲通假字高考题）；用户虽6次用"以理杀人"批礼教（电脑辩证/正本清源），但从未点名戴震。块52摘要里的"戴震→章黄求本字""《孟子字义疏证》反禁欲"是AI做摘要时用自身学术知识补的归宗，原文无据。
**根因分析**：地毯块摘要、逐块累积摘要、各类AI综述都是**二级文本**——AI在压缩时会"顺手"用自己的知识给用户的零散表述补一个学术来历（用户讲训诂/气论/反禁欲，AI就归到戴震名下）。这种归宗作为"学术联想"有价值，但一旦被下游当成"用户研究过X、某概念前史是X"的**事实**写进研究报告，就构成无来源的事实断言。L025管的是"时间首创性"（是不是第一次），L026管的是"来源归属性"（这话到底是谁说的、用户有没有真说过X）。
**修复措施**：迭代14在生命语义论纲领4.3专节做证据澄清——戴震降级表述为"AI可客观建立的汉语学术同构资源"，而非"用户走过的思想前史"；用户侧真实可证伏流只保留"重视字义训诂的一般倾向＋以理杀人的礼教批判"；同构分析照常做（由字通词通道≈三层拆解，确为严格同构），但严格区分"客观学理同构"与"用户主观师承"。
**预防机制**：
1. **凡引用二级摘要（块摘要/累积摘要/综述/索引）里的人名、书名、学派、引文做事实断言，必须回原始对话grep该专名0命中/命中情况；0命中则只能写成"AI建立的学术对照"，禁止写成"用户研究过/某概念源于"**
2. 区分两种表述：客观学理同构（A思想与B思想结构相通，不需要用户读过B）vs 主观师承前源（用户确实读过/研究过B，需原文证据）；前者可由研究者建立，后者必须有用户原文
3. AI做摘要时，凡"用自己知识补的学术来历"必须加标注（如"〔AI归宗，原文未点名〕"），不与用户原话混写
4. 扫描专名优先用Python（s.count），grep对超长行/特殊字符可能误报"命中文件"却取不到匹配，二者结果不一致时以Python逐字计数为准

---

## L027 · 个人信息检查只覆盖手机号/身份证，漏检"组合式身份关联泄露"（2026-08-30 第五次巡检）

**错误描述**：第五次巡检发现，AI生成的地毯分析/深度分析/迭代报告中明文包含学者真实姓名＋邮箱（[学者姓名及邮箱已脱敏]）、用户家族身份关联（[家族身份已脱敏]）、祖先八字与生辰，而库为公开GitHub库。此前多次巡检的"个人信息泄露检查"只扫描手机号正则、身份证正则、邮箱正则，对"真实姓名+学术邮箱+家族身份+八字生辰"这种**组合式身份关联**完全漏检。且这些信息已在公开库中存在一段时间。

**根因分析**：
1. 检查清单的"个人信息"定义过窄——只想到技术标识符（手机号/身份证/邮箱），没想到"姓名+单位+家族关系+生辰"组合起来同样可定位个人、且敏感度更高。
2. AI做地毯分析时直接复述用户对话中的隐私，未做脱敏，而巡检机制没有"AI生成内容必须过隐私筛"这一环。
3. 原始对话（用户提供的原材料）和AI生成的笔记混在同一库里，没有区分"原材料可含隐私（用户自有）"和"AI复述必须脱敏"的处理规则。

**修复措施**：
1. 当场对AI笔记类9个文件68处做最小化脱敏（姓名→代号、邮箱/八字/生辰/家族身份→[已脱敏]），全库复查除原始对话外0命中。
2. 原始对话6文件300+处未擅动（用户原材料，需用户确认是否脱敏/移出公开库）。
3. git历史仍含旧版，建议用户最快止血=库转private，或git filter-repo清历史。

**预防机制**：
1. **归位手册检查清单增加"组合式身份泄露"扫描项**：真实姓名（尤其是学者/公众人物）＋单位/邮箱、家族身份关联、八字/生辰/精确生日、家庭住址、学校班级，任一组合出现即标P0。
2. **AI生成笔记的隐私筛**：凡AI对用户对话做摘要/分析/地毯式复述，输出前必须过一遍隐私串替换（用户真实身份信息→代号），原始对话文件除外（用户自有原材料）。
3. **公开库假设**：默认库是公开的，任何写入都按"会被陌生人看到"处理；不确定是否敏感的信息，先脱敏再写。
4. 巡检个人信息检查从"正则扫描"升级为"正则+专名+身份关联上下文"三层。
---
## L028 · 时间跨度写顺口约数"二十多年"，实际约六七年（2026-08-30 溯源笔记，用户当场质疑）
**错误描述**：写《生命语义论_从灵感到工程溯源》时，未查年龄就写"小学四年级的问题走了二十多年"。实际北原慢热2008年生、2026年8月未满18岁，小学四年级约9-10岁，私人表述录明确口径"从小学困惑算起约六七年"。错误数字差近三倍，被用户一句"二十多年？"当场抓住。
**根因分析**：
1. 把"从小学到现在"习惯性套上成年人视角的"二十多年"，用自身常识代替材料。
2. 私人表述录里已有出生年、年级、跨度三处现成事实，写作前没回查。
3. 违反"每个数字必须来自输入/信源/可复现计算"铁律，把约数写进了结论句。
**修复措施**：用户质疑后立即回查私人表述录，将"二十多年/埋了多年"改为"约六七年（约9-10岁到17岁）"，笔记内留数字校准记录。
**预防机制**：
1. 凡涉用户年龄、年份、时间跨度、"X年前/多年来"，先回查私人表述录/带戳材料，写可核对的起止，禁用无来源约数。
2. 溯源/编年类结论句的时间数字括号标来源（如"私人表述录第83条"）。
3. 写完自查所有数字能否指到具体材料。

---
## L029 · 用量后台分模型tab的顶部总量是跨模型Key级数字，判断"实际用了哪个模型"必须以脚本model字段＋该模型请求次数为准（2026-08-30，用户截图当场纠正）
**错误描述**：对账DeepSeek用量时，在flash标签页看到"消费¥42.63"，又读《使用策略》"批量用flash"（应然），就对用户说"夜间批量实际走的是flash不是pro"。用户切到pro标签页截图：pro有437次请求，且pro页与flash页顶部Tokens都是同一个151,397,063。核脚本：4个.py全部写死deepseek-v4-pro，.py中flash出现0次。实际跑的就是pro，判断错误。
**根因分析**：
1. 用策略文档的"应然"（推荐批量用flash）覆盖了脚本的"实然"（model=pro硬证据），grep已经看到4个pro却选择性忽略——本本主义。
2. 没分清用量后台界面层级：顶部大数字（Tokens/可能含金额）是API Key级跨模型总量，在每个模型tab都显示同一个；只有该tab标题模型的"请求次数/分模型明细"才是模型专属。
3. 1.51亿token仅¥42.63的真正原因是99.2%输入命中上下文缓存（缓存价极低），却被错误归因于"换了便宜模型flash"。
**修复措施**：当场向用户更正——主力模型=deepseek-v4-pro（脚本写死＋437次请求为证）；低成本来自上下文缓存命中而非降配模型。
**预防机制**：
1. 判断"实际跑了什么模型/配置"，只认可执行脚本/运行日志里的实然字段，策略文档的推荐不算证据。
2. 读用量后台先区分"Key级跨模型总量"与"模型tab专属指标（请求次数/分模型token）"，跨模型总量相同不等于两个模型都跑了这么多。
3. 成本异常先查缓存命中率、输入输出比，再谈模型档位，不急于用"换了便宜模型"作解释。

---
## L030 · 外部协同方案照单全收险些踩API硬伤：任何外部/他处方案的参数层必须先回官方文档核实再扬弃（2026-08-30，协同手册v2.0综合时自查拦截）
**错误描述**：用户分享的《豆包+DeepSeek协同落地方案》主张"开thinking做深研的同时设temperature=0关创意、并强制json_object、以GPU显存≤30%作验收"。若照抄即错：①DeepSeek官方明载思考模式下temperature/top_p/penalty静默无效（设了不报错但不生效），Together.ai更指出V4 Pro低温会"压垮推理轨迹、降低质量"，官方建议1.0；②官方不建议thinking.enabled与json_object同开；③DeepSeek是云端API不占本地GPU显存。
**根因分析**：
1. 方案"看起来系统完整"就容易被当成品照收，犯了本本主义——骨架对不代表参数层对。
2. 把通用大模型经验（temperature=0降随机、低温稳输出）错误套到"思考型模型"上，没核实思考档与采样档是两套机制。
3. 与L029同构：都是"应然/印象"压过"实然/官方与脚本证据"。
**修复措施**：综合时先做外部检索核实（官方Thinking Mode文档、Together、腾讯云、Eino官网），在v2.0手册第〇节列"硬伤排异表"逐条纠正，改为"A档深研(思考max/不传温度/不绑json/markdown双层)＋B档结构化(关思考/低温0.2/json)"分档方案，验收删GPU指标换token成本。
**预防机制**：
1. 任何外部方案/教程/他对话产物，凡涉及具体API参数、模型行为、框架选型，落地前必须回一手官方文档或实测核实，标注核实日期与来源，不凭"它写得很笃定"采信。
2. 思考型模型的参数分两套记：思考档（effort控制，温度无效）与采样档（温度/top_p生效），不混用。
3. 扬弃综合永远先列"排异表"（哪里与事实冲突）再谈吸收，先排异后扬气。

---

## L031 · 人机协作文档里"某经典/专名的词频"≠作者读过：判断作者掌握了什么只能看用户段，不能看AI正文（2026-08-30，用户一句"没看过"当场纠正）
**错误描述**：讲高二道家治国研学与全本卷十关系时，据研学文档词频（道德经250、庄子144、应帝王18、天地34、陈鼓应53）断言"研学逼你系统过了一遍道家原典，卷十贴着原文走靠的是这时打下的文本底子"。用户当场纠正"没看过"。分角色统计研学区间(L1-3200)：这些专名几乎100%在AI生成的综述正文里（道德经 用户段0/AI段192，应帝王0/18，天地0/20，陈鼓应0/45，黄帝四经1/59），用户自己的话里一次都没引过原文。
**根因分析**：
1. 把"文档里出现/高频"等同于"作者读过/掌握"，忘了研学文档是**人机协作文档**——原典是AI检索转述整合的，高频只证明AI引用了，不证明用户通读。
2. 与L026同型（二级文本的归属问题）：L026是AI给用户"补学术来历"，本条是AI把"自己整合进来的知识"错记成"用户的阅读史/思想来源"，都会系统性高估作者的文本积累。
3. 违背用户一贯生产方式（8/26自述：凭哲学直觉建构、学术训练近乎零、内容靠和AI对话生成）——本该据此先存疑，却顺着"做综述=读原典"的常识想当然。
**修复措施**：当场撤回"文本原料层/打下原典底子"的因果；研学对卷十的真实遗产修正为只剩**问题意识与立场层**（抓住无为=不妄为顺规律、民本自化的精神；"不拿传统给现实找凭据、要独立思考"的方法论自觉；传统×马克思主义怎么接的问题意识），具体原典文本在研学和卷十都主要靠AI即时调取，不构成跨期文本依赖。卷十抬举庖丁（研学0次）恰证：选什么材料由生命论的新问题决定，而非吃当年材料库存。
**预防机制**：
1. **做思想发生史，凡断言"用户读过/研究过/掌握了某文献"，必须做分角色统计：该专名在"用户段"是否出现；只在AI段出现的，只能写"AI整合的客观资源"，禁止写成用户阅读史/师承/底子。**
2. 协作文档拆三层记账：AI整合的客观知识（不属于用户记忆）／用户提出的问题与判断（真正的思想）／共同落地的结论。跨期传承只认后两层里属于用户的部分。
3. 材料"随时可由AI现取"，因此"早年文档含X"不构成"后来成果依赖早年读过X"的因果；判断跨期依赖要找用户段里一以贯之的**问题意识**，不是文本重合。
4. 用户本人对"我读没读过/做没做过"是最高事实权威，被纠正时立即分角色回原文验证、全库同步修正，不辩解（同L024预防4）。
5. **【双向边界·用户2026-08-30二次精确】分角色词频只能识别"哪些是AI整合进来的"，既不能正向证明用户读过（AI段高频≠读过，本条主错误），也不能反向证明用户没读过——《道德经》在研学用户段同样0次，但用户确认亲读过（读了未必在该对话引用）。故阅读史最终只以用户本人确认为准，词频统计永远只是旁证。已确认事实：道家原著北原慢热仅亲读《道德经》一部；庄子/《黄帝四经》/黄老/陈鼓应等全部系用户指挥AI深度思考研究产出。映射到卷十：老子部分（道/无为/上善若水/小国寡民批判）有用户第一手阅读底子，庄子部分（庖丁=缄默之知、应帝王、天地）是生命论成型后由AI调取的客观学理同构资源，非用户阅读师承，二者来源等级不同，

---

## 2026-08-31 记忆恢复第1轮教训（迭代进化机制启动）

### L025 · /tmp易失目录导致产出和日志全部丢失
**错误描述**：记忆恢复第1轮A档深研启动后异常退出，/tmp下的日志文件、产出文件、甚至Python脚本全部消失。进程结束后无法确认：调用是否成功、产出了什么、花了多少token。所有痕迹灰飞烟灭。

**根因分析**：
1. 把所有中间文件（日志、产出、脚本）都放在/tmp，这个目录在容器环境中可能被定期清理或在执行阶段切换时回收。
2. 没有把产出直接写到持久目录（mingbenlun_local或仓库），而是先写/tmp再打算复制——中间环节断了就全丢。
3. 与L020（run_in_background长进程在新执行阶段被清理）是同一类问题：环境不可靠，不能赌临时目录和单次进程存活。

**修复措施**：
1. 所有DeepSeek产出**直接写到持久目录**（mingbenlun_local/记忆恢复/或仓库docs/），不经过/tmp。
2. 日志文件也写持久目录，不写/tmp。
3. 脚本本身放在仓库scripts/目录，不放在/tmp。

**预防机制**：任何预计超过5分钟的任务，产出和日志必须直接落持久目录，禁止使用/tmp作为中间存储。

---

### L026 · 代谢日志只在成功后写入，失败调用无痕迹
**错误描述**：ds_collab.py的record_metabolism()在call_deep()返回后才执行。如果API调用中途异常退出（网络断开、进程被杀、超时），代谢日志中没有任何记录。出现"好像调用过，但什么痕迹都没有"的情况。

**根因分析**：
1. 记账逻辑放在调用成功路径上，失败路径没有记账。
2. 没有"预记账"机制——调用发起时不记录，只有成功后才记录。
3. 这导致成本核算不完整，失败调用的token消耗无法追踪。

**修复措施**：
1. call_deep()发起前先写一条"启动中"记录到代谢日志（时间、实例、档位、对象、预估输入token）。
2. call_deep()返回后更新这条记录（实际命中/未命中/输出token）。
3. 如果异常退出，"启动中"记录保留，可通过时间戳和进程状态推断失败。

**预防机制**：所有API调用必须"先记账、再调用、后更新"，不允许只在成功路径记账。

---

### L027 · 前缀缓存有时间窗口，跨任务系列不命中
**错误描述**：深夜考古A档R2达到99.5%缓存命中（12416命中/64未命中），但仅12分钟后启动的记忆恢复B档却是0%命中（37992全部未命中）。前缀缓存没有跨任务系列复用。

**根因分析**：
1. DeepSeek前缀缓存不是永久的，有时间窗口（约10分钟）。超过窗口后缓存失效。
2. 不同任务系列之间即使使用相同的系统前缀，也可能因为缓存过期而不命中。
3. 记忆恢复B档输入37992 token（全部材料合并61KB），比深夜考古B档的2415 token大得多，即使前缀部分命中，大量材料内容也是新的。

**修复措施**：
1. 同一任务系列的B档和A档必须**连续调用**，B档结束后90秒内启动A档，最大化缓存命中。
2. 不同任务系列之间不假设缓存复用，每轮第一次调用按0%命中预算。
3. 材料切块后再喂给B档，不一次性合并61KB全量材料。

**预防机制**：批量任务跑之前先做缓存窗口规划，同系列调用连续跑，不穿插无关操作。

---

### 迭代进化机制（第1轮启动，持续到第11轮）

**原则**：不盲目自信，不一次性定死规则。每一轮跑完后拿真实结果复盘，把教训烧成下一轮的改进。

**每轮必做三件事**：
1. **复盘**：这一轮做了什么、遇到什么问题、哪些地方比上一轮好、哪些地方更差。
2. **沉淀**：把新教训写入教训库（编号L0XX），把改进措施写入对应脚本或手册。
3. **验证**：下一轮启动时，先检查上一轮的改进措施是否真正落实，用结果验证。

**进化轨迹（从第1轮开始）**：
- 第1轮（记忆恢复）：发现/tmp丢失、代谢日志不完整、缓存窗口问题 → 第2轮改进：持久化产出、预记账、连续调用
- 第2轮：待执行，预期发现新问题 → 第3轮改进
- ...
- 第11轮：基于前10轮全部成果，自己检索、自己定方向，做质的飞跃

**核心信念**：每一轮的失败都是下一轮的台阶。不害怕失败，害怕的是失败了不留痕迹、不改进、下一轮重蹈覆辙。写传统论须分别标注。**

---

## L032 · A档max_tokens=16000被思考链吃光导致正文硬截断，一天5次"花了钱只拿到半截产出"（2026-08-31，用户当场怒斥）

**事故**：A档payload `max_tokens=16000` 是【思考链reasoning + 正文content共享上限】。reasoning_effort=max时思考链常达1.6万–2万字，把额度几乎吃光，正文写到一半被API硬切（finish_reason=length），而旧脚本根本不读finish_reason，被切了无任何告警，半截产出被当成成品整合进报告。

**当天受害清单（5次，思考链越长正文越短）**：牛逼性论证（思考链19756字/正文仅5113字，断在第4层）、答案迭代2-3（16561/8639）、答案迭代4-5（18639/6754）、找回自己迭代3（19439/4183）、迭代研究8（17242/6983）。规律：思考链≥1.6万字必切。

**根因（三层，全中）**：
1. 参数层：max_tokens=16000对max档深研严重不足；实测v4-pro接受65536。
2. 检测层：_stream只累加delta，从不捕获choices[0].finish_reason，length截断静默。
3. 纪律层：L023早已写明"超长输出须分段、单次正文目标超8000字就分段"，没有遵守——重蹈覆辙。

**修复（已落地scripts/ds_collab.py）**：
1. A档max_tokens 16000→65536，B档8000→16000。
2. _stream捕获finish_reason；若=="length"，正文末尾追加【!!截断告警!!】并打印，明令"钱已花但产出不完整必须分段重跑，禁止拿半截当成品"。
3. 后台铁律：A档深研一律nohup脱离+日志轮询（前台会在思考期间被SIGTERM杀，exit143），不设硬性kill时间（用户原则：设硬时间是对自己规划不自信）。

**配套教训——喂料不足与喂料过度是同一种错误的两面**：为省钱把35万字压成30条摘要喂DS，DS只能做形式审查（逐条判"材料不足无法核验"、组合判据"未触发"），结论虽诚实但无法实质判断。正确做法是ReAct精准取数：B档先定位缺口→豆包白名单取对应**原文块**（不是全文也不是摘要）→A档深研。省钱=精准切块，不等于喂摘要。

**自检口令**：每次A档调用后必查 (a)有无截断告警 (b)正文是否覆盖任务要求的全部小节 (c)结尾是否完整收口。三者缺一即重跑，不得整合半截产出。

---

## L033 · 2026-08-31 · 喂料缺形式化原文导致研究员用软理由硬推（P编号攻关X/Y）
- **现象**：P编号攻关中，DS对张力X（明性=f³）、张力Y（f³截断无f⁴）只给出"命题级"裁决，Y还依赖"耦合=水平"的词义软推断并自承软肋。北原慢热追问"你怎么证明"后回库核验，发现库里本有定理33（明(明(A))≅明(A)，f³不动点，G^ω(C)紧闭合范畴形式化+PTL解释器8过10拒），X本是定义级明言。
- **根因**：B档取数只喂了《哲学坐标定位》（二手综述）和三件事方案，没grep卷一篇三/卷三篇一/卷十一篇四的定理原文与定理编号表。研究员在缺硬证据时诚实地降级为命题并标注软肋（DS行为正确），但取数不到位让结论强度被白白压低、还差点让作者为"库内已有答案的问题"拍板。
- **教训（铁律）**：凡涉及"是不是定理/能不能证明/严谨度定级"，取数第一步先 grep 全库定理编号表（总纲"定理NN"）+卷十一PTC/PTL形式化篇+关键词"不动点/证明/≅"，把形式化原文与哲学表述一起喂；二手综述只能作索引不能作最终证据。
- **次生收获**：核验新抓到"第四层集体明性（尺度维度）vs 没有f⁴（f层级）"的同名词冲突，需在总纲区分垂直f层级与水平尺度层。

---

## L034 · 2026-09-03 · 长期自设 DeepSeek 枷锁、越位当有损翻译层（范式革命综合教训）
- **现象**：很长时间里反复出现——DS"空输出/被掐断"就重开对话、为省钱把上下文裁成几百行、max_tokens 拍脑袋设 8192/32768 甚至砍到 8000、让 DS"只给思路"再由 S04 翻译成代码、S04 自己硬扛写 tactic、多份互相矛盾的 DS 方法论并存把自己绕晕。
- **根因（三层）**：①没读官方最新文档、没做小成本实测，把 V3.2 时代的窗/输出上限（128K/8K）当成永恒约束，还误把"reasoning 吃额度导致空输出"误诊为"reasoner 不适合长输入/被掐断"；②把"省钱"置于"一次做对"之前，犯了自己批判过的抓小放大（缓存最次要，返工才是最大成本）；③角色越位，S04 抢了 DS 的证明书写权，当了必然损耗信息的翻译层。
- **铁律**：(a) 任何 API 参数先查官方文档+小成本实测，用数据定不凭记忆；(b) V4 全量喂三层全文（约49K token 占1M窗4.9%），证明用 v4-pro+thinking+max_tokens≥32000+自动续写；(c) DS 主谋出完整代码，S04 只做 coqc/git/哲学桥梁，不手改 tactic；(d) 唯一权威文档取代多份矛盾旧文档，旧文档归档留痕。
- **落地**：`docs/协作机制/分站/S04_DeepSeek_V4新范式调用规范.md`、`scripts/s04_deepseek/`、旧文档归档 `_归档_旧范式_20260903/`、结晶013/014/015。
